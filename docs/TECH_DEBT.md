# Технический долг — Tenzosila KP & Dogovor

Приоритизация: **A** — фиксить первым (молчит и врёт в клиентском документе / гейт
деплоя). **B** — следующим (хрупкость, реальные дыры, но не врёт прямо сейчас).
**C** — потом (косметика, принятый MVP-скоуп, крупные рефакторинги).

Статус пункта — в заголовке: ✅ ЗАКРЫТО / 🕵️ ЛОЖНАЯ ТРЕВОГА (описание было
устаревшим или неточным, реального дефекта не найдено) / 🔲 ОТКРЫТО / 🔒 ОТЛОЖЕНО
(осознанное решение не делать — не считается открытым долгом). Обновлять статус
сразу после фикса или проверки — не копить расхождение с STATUS.md.

Каждый открытый пункт — с описанием проблемы и данными, достаточными для фикса без
дополнительной разведки. Перед фиксом сверить актуальность номеров строк (код мог
сдвинуться) и прогнать `python -m pytest tests/ --ignore=tests/contracts/synthetic`
до и после.

---

## Прогресс

| Раздел | Закрыто | Ложная тревога | Открыто | Отложено | Всего |
|---|---|---|---|---|---|
| A | 2 | 1 | 0 | 0 | 3 |
| B | 0 | 1 | 6 | 0 | 7 |
| C | 3 | 1 | 13 | 21 | 38 |

**Раздел A — пуст по существу.** Оба реальных пункта закрыты, третий — ложная тревога.
Гейт деплоя снят.

**Остаток реального долга (🔲):** A0 + B6 + C13 = **19**. Отложенное (🔒) в счёт
не идёт. Быстрый пересчёт — `grep -c "🔲" docs/TECH_DEBT.md`.

---

## A — в первую очередь

### A1. Привязка «ТПК»→«Компания» к подстроке номера Госреестра (C4) — 🕵️ ЛОЖНАЯ ТРЕВОГА

**Диагноз (2026-07-11):** привязка матчит актуальный номер `78871-20`
(`src/contracts/filler.py:55`, `_GOSREESTR_MARKER`). Фикс был применён раньше, чем
этот пункт попал в файл (коммиты `8adff78`/`414bd7d` — тест-ловушка и фикс — оба
предшествуют записи в TECH_DEBT.md). На свежесгенерированных документах замена
срабатывает, `test_c4_contractor_name_forms_sentinel` зелёный (44 passed). Запись в
файле была превентивной, а не наблюдением реального сбоя. Фикс не требуется.

---

### A2. «Распознать» перетирает ручной ввод реквизитов (P1-8) — ✅ ЗАКРЫТО

**Диагноз (2026-07-11):** описание пункта было неточным — вызов уже шёл через
`merge_requisites`, а не `set_requisites`. Реальный баг: `merge_requisites`
перезаписывал ЛЮБОЕ поле, для которого новый текст дал совпадение, включая
поля, введённые вручную; защиты не было ни в UI-обещании («уже заполненное не
затирается»), ни в тесте `test_dogovor_requisites_reset.py`.

**Фикс:** `sync_field` (`src/contracts/state.py`) помечает первичные поля
`requisites_manual` при реальном пользовательском вводе (on_change не
срабатывает на программную запись); `merge_requisites` пропускает такие
ключи. Производные поля (ФИО в РП, инициалы) в этот набор не попадают и
всегда пересчитываются от актуального (в т.ч. защищённого) первичного поля.
Поле «Пол директора» защищено той же семантикой (отдельный добивающий коммит).
Два новых теста в `test_dogovor_requisites_reset.py`, старые не менялись.
Коммит `defb5c5` + добивка по полу.

---

### A3. `use_container_width` → `width=` — устаревший API Streamlit — ✅ ЗАКРЫТО

**Фикс (2026-07-11):** 3 вхождения `st.data_editor(use_container_width=True)`
заменены на `width="stretch"` (`src/admin/price_update_view.py`,
`src/app_pages/2_Договор.py`, `src/ui/payment_lines_editor.py`). Гейт деплоя
снят. Коммит `70c85d9`.

⚠️ Побочная история: после этого фикса была замечена асимметрия в блоке
«Реквизиты заказчика» на странице Договор — расследование показало, что причина
НЕ в этом фиксе (регресс воспроизводился и до него), а в исходной раскладке блока
(разная высота `text_input`/`text_area` в двух колонках-стопках без построчного
выравнивания). Починено отдельно (см. «Закрыто в сессии» в STATUS.md).

### A4. `extractor.py` — LLM с правом менять числа и догадываться — ✅ ЗАКРЫТО

**Фикс (2026-07-11, минимальный — без редизайна):** коммиты `d52f0af`, `9659bd2`,
`4384e4d`.
- Промт `extract_contract_data.txt`: только дословное копирование. Убраны пересчёт
  сумм под ИТОГО, суммы прописью, падежи/инициалы, «Аванс»→«Предоплата»,
  «восстанови по контексту», конструирование наименований. Оставлены: ОСНОВАНИЕ
  в РП (решение Антона), форматы полей, структурный маппинг. Тест-страж на промт.
- Хардкод страниц 3–5 снят — режим B читает PDF целиком (`extract_kp_text` удалён).
- Пустой вход (скан) → `NoTextLayerError` ДО вызова LLM, понятная ошибка в UI.
- Преобразования делает код: `fill_missing_derived` (петрович, только в пустые
  поля) после извлечения; `number_to_words` для пустого СПЕЦ_ИТОГО_ПРОПИСЬ при
  генерации. Нормализация текстов оплаты (пропись в пунктах, НДС в каждый пункт)
  убрана БЕЗ замены кодом — менеджер правит поля формы (решение Антона).
- `extract_card_data()` и его промт не тронуты (решение Антона: LLM-извлечение
  карточек остаётся), добавлен только guard на скан.
- Не сделано (при переносе в `core/`, P0-21): structured output + Pydantic,
  статус поля с цитатой, vision для сканов, ретраи/таймауты, модель в settings.

Исходная проблема (4 дефекта тихого вранья: пересчёт сумм LLM-ом, «восстанови
по контексту», хардкод страниц 3–5, слепота к сканам) — полное описание в
`git show c22a75c` и коммитах фикса.

---

---

## B — следующим эшелоном

### B1. Два семейства id в матчинге оплаты — 🔲 ОТКРЫТО (не фиксить превентивно)

**Проблема:** путь Спецификации использует канонические id из `spec_items`
(`rama`, `pandus`, `orion_poles`), путь КП — ключи опций конфигуратора.
`wording_flags` матчит оба семейства параллельно. Не баг сейчас, но при
добавлении НОВОЙ позиции легко забыть завести id в одном из двух семейств —
формулировки оплаты (W6, W9 и т.п.) перестанут матчить эту позицию молча.

**Данные для фикса (держать в уме при следующей новой позиции, не чинить превентивно):**
- Искать: `grep -rn "wording_flags" src/`
- При добавлении новой позиции — чек-лист: id заведён и в `spec_items`
  (канонический), и в ключах опций КП; `wording_flags` матчит оба.
- Кандидат на устранение позже: единый реестр id вместо двух параллельных
  семейств — отдельный рефакторинг, не делать между делом.

---

### B2. Р/с-checksum в валидаторе реквизитов — неполный — 🔲 ОТКРЫТО

**Проблема:** валидация расчётного счёта сейчас проверяет только длину (20
цифр) и согласованность с БИК через к/с. Полная 23-значная контрольная сумма
р/с (алгоритм ЦБ РФ 7-1-3) не считается — невалидный по контрольной сумме
счёт может пройти валидацию и попасть в договор.

**Данные для фикса:**
- Искать: `grep -rn "_valid_inn\|requisites_validation" src/` — валидатор
  реквизитов, туда же добавляется чек-функция.
- Алгоритм ЦБ РФ для проверки р/с: контрольная сумма считается по 23-значному
  ключу (БИК-код банка, дополненный нулём слева при необходимости, + номер
  счёта), с весовыми коэффициентами 7-1-3-7-1-3-7-1-3-7-1-3-7-1-3-7-1-3-7-1-3-7-1
  (повторяющийся паттерн), сумма произведений по модулю 10 должна быть равна 0.
  Реализовать как отдельную чистую функцию, покрыть юнит-тестами на известных
  валидных/невалидных пар БИК+счёт.
- ⚠️ ВАЖНО: `_valid_inn` импортируется из парсера в `requisites_validation.py` —
  НЕ переименовывать при рефакторинге этого модуля, иначе молча сломается
  импорт в валидслое.
- Тест: несколько известных валидных пар БИК/р-счёт (банковские примеры из
  открытых источников) + заведомо невалидная контрольная сумма → функция
  должна корректно отличать.

---

### B3. vMerge ячеек в таблице Спецификации DOCX — 🕵️ ЛОЖНАЯ ТРЕВОГА

**Диагноз (2026-07-11):** описание было неточным — относилось к таблице позиций
КП (не Спецификации договора), к столбцу «Срок исполнения» (объединение ячеек
весов + опций-наследников при параллельном монтаже). Баг чинили четыре раза в
апреле-мае 2026 (`src/generators/spec_vmerge.py`), последний известный симптом —
дублирующиеся значения в объединяемых ячейках. Проверено на свежем КП с составом
«весы + рама + ограждение + фундамент»: vMerge отрабатывает корректно, одна
объединённая ячейка на весь параллельный блок. Баг не воспроизводится, фикс
не требуется.

---

### B4. `merge_runs` guard не защищает `w:pict`/`w:object`/field-runs — 🔲 ОТКРЫТО

**Проблема:** функция слияния текстовых runs в DOCX (используется, вероятно,
для нормализации форматирования после подстановки плейсхолдеров) имеет guard
против повреждения `w:drawing` (изображения), но НЕ защищает `w:pict`
(legacy-картинки), `w:object` (OLE-объекты) и field-runs (динамические поля
Word, например номера страниц). Риск — повреждение этих элементов при
слиянии runs.

**Данные для фикса:**
- Искать: `grep -rn "merge_runs" src/`
- Расширить существующий guard (уже есть для `w:drawing`) — добавить
  аналогичные проверки на присутствие `w:pict`, `w:object`, `w:fldSimple`/
  `w:fldChar` в run перед слиянием; если элемент присутствует — run не трогать.
- Тест: собрать DOCX с полем/legacy-картинкой (или мокнуть XML-структуру),
  прогнать через `merge_runs`, убедиться, что элемент не повреждён/не потерян.

---

### B5. Реальное удаление позиций прайса через UI (сейчас no-op) — 🔲 ОТКРЫТО

**Проблема:** в админ-панели прайса кнопка удаления позиции визуально
присутствует, но фактически ничего не делает (no-op) — UI вводит пользователя
в заблуждение, будто действие выполнено.

**Данные для фикса:**
- Искать: страница админки прайса, кнопка удаления —
  `grep -rn "удали\|delete.*price\|remove.*price" src/`
- Реализовать реальное удаление из `data/prices.json` (JSON-справочники
  правятся вручную по CLAUDE.md — уточнить с Антоном перед автоматизацией
  записи в файл, это может требовать отдельного согласования).
- Либо, как минимальная альтернатива: убрать кнопку/задизейблить с пометкой
  «недоступно», пока реальное удаление не реализовано — не оставлять
  вводящий в заблуждение no-op.

---

### B6. P2-кластер парсера реквизитов (робастность распознавания) — 🔲 ОТКРЫТО

**Проблема:** несколько связанных недоработок парсера реквизитов из аудита
AUDIT_2026-07: OCR-нормализация путает похожие символы (О→0, З→3, l→1);
склейка номера счёта с пробелами не обрабатывается; метка «Адрес:» не
срезается из значения при извлечении; «факт» адрес не мапится в поле
АДРЕС_ПОЧТ; при двух р/с в документе используется правило first-wins без
проверки корректности; `_blank_span` может случайно съесть кавычки в имени
организации, если банк указан раньше названия в тексте.

**Данные для фикса:**
- Искать парсер: `grep -rn "_blank_span\|АДРЕС_ПОЧТ" src/`
- Каждый под-пункт — отдельный небольшой юнит-тест на конкретном примере
  документа/строки, воспроизводящем проблему, ДО фикса (иначе легко починить
  один кейс и сломать соседний regex).
- Рекомендуется заводить как отдельные маленькие PR/коммиты по одному
  под-пункту, не одним большим патчем — регекспы парсера легко конфликтуют.

---

### B7. Дефект 3: `grand_total` per-unit при qty>1 — 🔲 ОТКРЫТО (латентный, маскирован)

**Проблема:** в `src/contracts/from_kp.py` `grand_total` суммирует per-unit цены
позиций без умножения на `model_qty` — при qty>1 итог занижен. Дефект латентный
(на практике qty=1), помечен в коде комментарием-маркером «латентный дефект при
qty>1; здесь только display». Значение уходит в `СПЕЦ_ИТОГО` и
`СПЕЦ_ИТОГО_ПРОПИСЬ`.

**Данные для фикса:**
- Искать: `grep -n "grand_total" src/contracts/from_kp.py` (номер строки не
  хранить — плывёт).
- Воспроизведение: фикстуры qty=2 уже есть — `tests/autoverify/fixtures/*_x2.json`.
- Тест-инвариант сумм до/после — обязательно (payment-путь рядом).

---

## Отдельно закрытые дефекты (не были в исходном A/B/C, всплыли по ходу)

### И-4. Ручные позиции (custom_item) невидимы для рендера клауз — ✅ ЗАКРЫТО (частично)

**Проблема:** ручная позиция «Монтаж» (custom_item, не опция) не попадала в
обязательства сторон договора — договор уходил клиенту без пунктов про автокран,
площадку, простой бригады, хотя монтаж был оплачен.

**Фикс (2026-07-11):** реестр `CUSTOM_WORK_TYPES` (`src/contracts/custom_work_types.py`),
селект «Тип работ» в UI ручной позиции, продвижение до канонического
`id="installation"` + `metadata["scope"]` при выборе типа «Монтаж». Клаузы
рендерятся, блок оплаты не затронут (доказано трассировкой payment-пути + тестом).
Legacy-снапшоты (без поля scope) → «Прочее», рендер идентичен старому поведению.
Golden `G-09` перезаморожен осознанно (клаузы монтажа добавлены, блок оплаты без
изменений, `G-01` не тронут). Ревью свежей Opus-сессией на дифф — пройдено.

**Не закрыто (осознанно, см. пункт ниже в разделе C):** та же дыра для ручных
Фундамент/Поверка/ОРИОН — реестр расширяемый, но эти типы пока не заведены.

---

## C — потом (косметика / принятый скоуп / крупные рефакторинги)

Ниже — пункты с низким риском и низкой срочностью. Фиксить по мере
появления свободного слота, не между активными фронтами.

### UI / UX страницы Договор — ✅ ЗАКРЫТО (2026-07-11)

- [x] «Тип документа» перенесён внутрь секции «Параметры договора».
- [x] Ошибки реквизитов перенесены в сайдбар (унифицировано с КП-страницей).
- [x] Пункт «app» убран из навигации сайдбара.
- 🔒 Не трогали (осознанно): локализацию file-uploader («Drag and drop file here»
  и т.п.) — плохо локализуется в Streamlit, не стоит усилий. Пропорции поля/кнопки
  «Найти» — вкусовщина.

---

### Реестр типов работ для ручных позиций (custom_work_types) — 🔲 ОТКРЫТО

- [ ] **Расширить `CUSTOM_WORK_TYPES` на Фундамент / Поверку / ОРИОН.**
  Сейчас реестр (`src/contracts/custom_work_types.py`) наполнен только `Прочее` +
  `Монтаж` (см. И-4 выше — закрыт единственный задокументированный случай).
  Механизм общий и расширяемый: добавление типа = одна запись в реестре, без
  правок `clauses_context.py` / `clauses.yaml` / `from_kp.py`.

  **Ограничение в MVP — осознанное, не забывчивость.** Дефолты фундамента «тяжёлые»
  (тянут пакет обязательств: ровная площадка, несущая способность грунта,
  стройзадание, контрольный лист, приложения с чертежами, зимний бетон). Ручную
  позицию менеджер создаёт именно тогда, когда случай НЕСТАНДАРТНЫЙ — вкатить ему
  стандартный пакет фундаментных обязательств хуже, чем не вкатить ничего: пустой
  раздел менеджер заметит, а неверные клаузы выглядят правдоподобно и уедут клиенту.
  Клаузы монтажа в этом смысле безопасны — универсальны для любого монтажа.
  У фундамента/поверки/ОРИОН при этом богатый UI штатных опций, куда логичнее
  вносить их, а не вбивать руками.

  **Текущее поведение ручных Фундамент/Поверка/ОРИОН (тип «Прочее»):** строка в
  спецификации есть, в ИТОГО входит, в блок оплаты попадает (бакет по эвристике
  имени, `_payment_group_by_name`) — но обязательства сторон в договор НЕ
  рендерятся. Это тот же класс дефекта, что чинили для монтажа, просто не
  задокументированный на практике.

  **Что делать при расширении** (заготовки — закомментированные строки в реестре):
  1. Раскомментировать нужную запись `WorkType(label, spec_id, scope, payment_group)`.
  2. ⚠️ **Сверить `scope` и `payment_group` со значениями, которые кладёт
     соответствующая ШТАТНАЯ ОПЦИЯ** (`from_kp.py` — metadata позиции опции;
     `resolve_payment_group` — бакет). Если разойдутся — ручная позиция и опция дадут
     разные клаузы/разные бакеты оплаты при одинаковом смысле. Это главный риск.
  3. Для фундамента дополнительно решить: какой `foundation_scope` считать дефолтом
     (`contractor_full` / `customer_builds` / иное) — от него зависит, кто по договору
     строит фундамент. Неверный дефолт = договор с чужими обязательствами.
  4. Поверка: проверить `customer_side`-клаузы (испытательный автомобиль, организация
     поверки) — они могут требовать отдельного флага помимо `scope`.
  5. Тесты по образцу монтажа: продвижение id, legacy без поля scope, коллизия
     опция+ручная позиция того же типа, payment-инвариант (бакет не поехал),
     golden-фикстура с осознанным re-freeze.

  **Приоритет: низкий.** На практике не воспроизводилось (менеджеры вносят фундамент/
  поверку/ОРИОН опциями). Делать по факту реального случая, а не превентивно —
  иначе рискуем вкатить «тяжёлые» дефолты туда, где они не подходят.

---

### Оплата / формулировки
- 🔒 **W1** — составная строка «Весы и доставка» в Спецификации остаётся flat
  (осознанно, не баг). Расщепление показ-%/расчёт-суммы в `PaymentLine` —
  риск для инварианта сумм, делать отдельным фронтом под замороженной сетью,
  opus, plan mode, обязательный новый тест-инвариант до/после.
- 🔒 **Легаси бандл ОРИОН в пути оплаты** — не расщеплён (`orion_lite/standard/
  auto` целиком падают в бакет «весы»). Риск сумм при расщеплении, отдельный
  фронт с разведкой.
- 🔲 Ручные суммы-копейки в редакторе оплаты — режим поверх основного расчёта.
- 🔲 P2: `payment_lines_editor` группирует % по тексту «Объект» — сделать
  привязку по стабильному bucket/source id вместо текстового матчинга.
- 🔒 `resolve_payment_group` для `orion_cable_poles` относит опоры в бакет
  `foundation` без проверки, есть ли физический фундамент в сделке (см. тикет
  S6, уже прикрыт UI-уведомлением с рекомендацией менеджеру — правка самой
  бакет-логики отложена намеренно).

### Генерация / клаузы
- 🔲 E2: нумерация клауз размазана — нумеруется с 4 в коде, «2.»/«3.» литералами
  в шаблоне. Свести к одному источнику.
- 🔒 `normalize_quotes`: граница работает под одну вложенную сущность в кавычках
  (подтверждено — имена всегда одна сущность, реальные данные не задевает).
- 🔲 `ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА` — вынести константу «не более 50 м» явно.
- 🔲 `section_number` в `data/clauses.yaml` — определён, но рендерером не
  используется.
- 🔒 `winter_concrete` — пересмотреть после будущего scope-редизайна фундамента.

### Парсер реквизитов
- 🔲 P1-7: валидация ОГРН в парсере. ⚠️ Мина: эталонный ОГРНИП
  `316619600054321` в `test_requisites_parser.py` невалиден по контрольной
  сумме — заменить на валидный (например, `304500116000157`) ДО того, как
  парсер начнёт валидировать ОГРН, иначе существующий тест сломается.
- 🔲 P1-6: статусы «нашёл/не нашёл/не уверен» в UI парсера (низкий приоритет,
  острие уже снято валидационным слоем).
- 🔒 Natasha (NLP-библиотека) для распознавания ФИО/адресов — опциональное
  усиление, если текущий regex-парсинг окажется недостаточно надёжным на
  реальных документах. Не делать превентивно.

### Рендеринг / DOCX
- 🔲 P3: временный файл не удаляется при падении `fill_template` в `compose.py`
  (нужен try/finally с очисткой).
- 🕵️ `docs/supply_payment_preview.txt` — пересохранить в UTF-8 (из ревью v2.2).
  **Диагноз (2026-07-11):** файл уже UTF-8 (с BOM, `EF BB BF`), читается
  корректно. Ложная тревога либо давно закрыто без отметки.

### Тесты — 🔒 ОТЛОЖЕНО
- 4-5 падающих `test_e2e_synthetic`/`case_*` фикстур — устарели, дрейфуют.
  Если всплывают как modified в `git status` — `git restore`, НЕ коммитить
  вслепую. Разобрать отдельным контрактным треком, не чинить по одному.

### Крупные рефакторинги (отдельные фазы, не «между делом») — 🔒 ОТЛОЖЕНО (все пункты)
- Вынос глюкода `2_Договор.py` → `src/contracts/generate_service.py` — под
  autoverify-сетью (сеть уже есть и зелёная), после закрытия текущих фронтов.
  **Prerequisite: golden-snapshot для autoverify** — сначала снапшот текущего
  вывода, потом вынос; без снапшота рефакторинг глюкода не начинать.
- Разбить крупную `src/app_pages/2_Договор.py` (>1000 строк) на модули —
  отложено осознанно, делать вместе с выносом в `generate_service.py`.
- `data_loader`: дублирование `equipment_specs` ↔ `models.json` — свести к
  одному источнику.
- Терминология `ЗАКАЗЧИК_*` → `КОНТРАГЕНТ_*` по всей кодовой базе — большая
  механическая задача, не срочная.
- AI-extractor реквизитов → миграция на DeepSeek после стабилизации текущего
  решения на OpenRouter.

### Фичи (бэклог) — 🔲 ОТКРЫТО (все пункты)
- Реквизиты из файла (DOCX/XLSX/PDF-текст, без LLM) — spike сначала.
- Ручная спецификация с загрузкой стройзадания.
- Зимний режим фундамента (+обогрев) и «материал заказчика» — проверить охват.

### Инфраструктура / хозяйство
- 🔒 Supabase temp-директория на Windows: `pytest` без флагов падает с
  `PermissionError` на временных файлах — известная локальная досада
  (деплой на Streamlit Cloud — Linux, там не воспроизводится).
- 🔲 `make_template.py` — устаревший путь `SRC` (`03_knowledge_base` →
  `knowledge_base`, каталог был переименован). Однострочный фикс пути.
  Bless-пайплайна для autoverify не существует (подтверждено отдельной
  разведкой) — скрипт не в критическом пути, чинить как обычный техдолг.

### Известные ограничения (НЕ баги — принятый MVP-скоуп, не фиксить) — 🔒 ОТЛОЖЕНО (все пункты)
- `named_form`/«ИМЕНУЕМ»: формы Колхоз/Компания/Фирма используют
  «именуемое» (не всегда грамматически точное согласование) — принято.
- Между двумя таблицами в DOCX нужен минимум один абзац-разделитель, иначе
  Word визуально сливает таблицы — известное ограничение python-docx/Word.
- ОРИОН-приложения (Шаг 10 мастера) не реализованы как отдельный пакет
  документов.
- Контрольный лист приёмки фундамента — только варианты «пандусный» и
  «приямок».
- `монолитная_плита` (тип фундамента) — без отдельного стройзадания нет
  унифицированных чертежей для этого варианта.
- Supply-флоу на `docxtpl` — при смене шаблонов требуется отдельная сверка
  плейсхолдеров (не автоматизировано, ручная проверка).
- Автовыбор типа договора в UI — только подсказка-дефолт (radio-кнопка),
  финальный выбор всегда ручной.

---

## Как использовать этот файл

1. **Обновлять статус пункта СРАЗУ после фикса или проверки** — не копить
   расхождение между файлом и реальным состоянием кода. Три пункта в этой
   версии (A1, B3) оказались ложными тревогами именно из-за такого разрыва —
   описание жило дольше, чем сам дефект.
2. Свериться, что номера строк/файлов актуальны (код мог измениться) —
   перепроверить через `grep`/`view`, не доверять слепо.
3. Написать/найти воспроизводящий тест ДО фикса, где это уместно
   (диагноз фактами до фиксов — принцип проекта).
4. Прогнать `python -m pytest tests/ --ignore=tests/contracts/synthetic`
   до и после — синтетика (`tests/contracts/synthetic/`) исключена из
   прогона намеренно, не трогать, не анализировать её падения.
5. Один пункт — один атомарный коммит (Conventional Commits, русский).
6. Для изменений бизнес-логики (платежи, клаузы, spec-рендереры) или
   многофайловых правок — ревью более сильной моделью до `git push`
   (см. правила ревью в `CLAUDE.md`).
7. Обновить таблицу «Прогресс» в начале файла при закрытии/открытии пунктов.
   **Закрытый пункт (✅/🕵️) сжимать до 3–5 строк** — диагноз + хэш коммита;
   полная история — в `git show` коммита закрытия, не здесь.
8. `grep "🔲" docs/TECH_DEBT.md` — остаток реального долга, цель фазы — ноль.
   🔒 ОТЛОЖЕНО (осознанное решение не делать) в счёт не идёт.
