# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-07-10

Только невыполненное и техдолг. История закрытых задач — в git log.

---

## Активная задача

**F1 — единый источник формулировок блока оплаты.** Ветка `f1-payment-wording`, запушена,
golden ещё НЕ блессен. Свод двух генераторов (КП lite / Спецификация full) в один словарь
`src/payment_wording.py`; дефолты только из `data/payment_terms.json`.

Рабочие документы (docs/): `FIX_SPEC_payment_wording_2026-07-09.md` (требования W1–W11,
решения приняты), `PLAN_F1_payment_wording_2026-07-09.md`, разведки `RECON_payment_*.md`,
`RECON_montage_drop_2026-07-09.md`, **`REVIEW_scenarios_2026-07-10.md`** (сверка с эталонами,
см. ниже — S5–S9 запрошены, отчёт ожидается).

**Сделано (запушено, golden не блессен), 1408 тестов зелёные:**
- Стадия 1+2 F1 (модуль-словарь, свод дефолтов, W3–W11), T1–T4 (контент КП), сжатие блока
  оплаты КП (слияние равных %, срок убран из КП, короткие события).

**🔍 Сверка с эталонами ПЕРЕД bless — в процессе (не пропускать).**
После серии правок решено проверить вывод на реальных билдерах (не UI) против 9 эталонов
`docs/contract_templates_v1/` — чтобы не заморозить регресс в golden. Метод: генерация через
`tests.autoverify.runner.generate_all()` (продакшн-функции, не LLM-сочинение), сверка
формулировок/структуры (не сумм) с эталоном, классификация [РЕГРЕСС]/[НАМЕРЕННО]/[ЭТАЛОН].

**S1–S4 проверены** (`docs/REVIEW_scenarios_2026-07-10.md`):
- S1 (фундамент+монтаж+поверка+доставка), S2 (шеф-монтаж), S4 (ОРИОН+опоры) — **чисто**,
  инвариант КП=договор держится, все W-пункты PASS или [НАМЕРЕННО] (ожидаемо).
- S2 дополнительно подтвердил: монтаж-drop (см. ниже) НЕ воспроизводится через билдеры —
  строка шеф-монтажа на месте на обеих сторонах. Баг живёт в session_state UI, не в генераторах.
- **S3 (простая поставка, prepay_100) — 1 подтверждённый регресс, 1 ложная тревога:**
  - **[РЕГРЕСС] подтверждён:** `src/contracts/supply_filler.py:295` —
    `number_to_words(total).capitalize()` не убран (F1 убрал только в `payment_line.py`).
    В одном договоре п.4.1 «Один миллион…» (заглавная), п.4.2.1 «один миллион…» (строчная) —
    рассинхрон. **Фикс: убрать `.capitalize()` в `supply_filler.py:295`.** Не применён —
    решено дождаться S5-S9 и чинить пакетом.
  - **[ЛОЖНАЯ ТРЕВОГА, НЕ ТРОГАТЬ]:** `prepay_100` печатает «Предоплата 100%», не
    «Оплата 100%» (правило W3). Отчёт пометил как регресс с оговоркой — **решено: НЕ баг.**
    W3 (слово «Оплата» для единичного платежа) распространяется ТОЛЬКО на `split_by_items`
    (решение зафиксировано в FIX_SPEC/STATUS). `prepay_100` — отдельный нерасщепляемый
    пресет, «Предоплата 100%» **совпадает с эталоном** простой поставки. Трогать нельзя —
    сломает совпадение с эталоном. Зафиксировано, не переоткрывать.

**S5–S9 — ЗАПРОШЕНЫ, отчёт ожидается** (промт отправлен, результат дописывается в тот же
`REVIEW_scenarios_2026-07-10.md`):
- S5 весы+монтаж+доставка+поверка (без фундамента) → `Автовесы_монтаж_gemini.md`
- S6 весы+монтаж+ОРИОН (без фундамента) → `Автовесы_монтаж_ОРИОН_gemini.md`
  ⚠️ **Особое внимание**: опоры ОРИОН (W9) по решению садятся в бакет ФУНДАМЕНТА — здесь
  фундамента нет. Куда попадает сумма/название вехи? Потенциальный рассинхрон, не проверено.
- S7 весы+рама+монтаж (обычный, не шеф) → `Автовесы_рама_монтаж_gemini.md`
- S8 фундамент из материала заказчика+монтаж → `Автовесы_строительство_с_предоставление_материала__монтаж__gemini.md`
- S9 фундамент+монтаж+зимний бетон → `Автовесы_фундамент_монтаж__в_зимнее_время_gemini.md`

**Монтаж-drop (КП № 223311, шеф-монтаж) — разобран, ложная тревога** (не баг билдеров).
Разведка + сверка S2 подтвердили: строка монтажа не теряется в продакшн-пайплайне. Причина —
протухший `st.session_state` при переключении КП в одной вкладке (баг (b), техдолг).
bless этим НЕ блокируется — autoverify зовёт билдеры напрямую.

**W1 — вынесена в ОТДЕЛЬНЫЙ фронт (после bless).** Составная строка «Весы и доставка» в
Спецификации (L3) остаётся flat намеренно — расщепление «показ-%»/«расчёт-суммы» в
PaymentLine рискованно для инварианта сумм. Подтверждено во ВСЕХ проверенных сценариях
(S1/S2/S4) как [НАМЕРЕННО], не регресс. Делать под сеткой, opus.

**Очередь:**
1. Дождаться отчёта S5–S9, разобрать в чате (разбор — следующий чат, см. промт ниже).
2. Пакетный фикс: W4 `supply_filler.py:295` (подтверждён) + любые новые [РЕГРЕСС] из S5-S9.
3. `codex review`/ревью более сильной моделью всей ветки.
4. **bless golden** (autoverify шаг 7) — после того как счёт регрессов = 0.
5. `git push`, мёрж в `main` при готовности.
6. Потом отдельными фронтами: W1 (opus, под сеткой), баг (b) session_state.

**Решено и почему (F1) — не переоткрывать:**
- Доставка 0/100, L3 печатает % весов (эталонная арифметика).
- Асимметрия защёлки L1 (весы+фундамент) vs L3 (весы+доставка) — осознанная.
- W7 (счёт) — только full, не в lite (КП).
- W6 scope — один источник (`installation_scope` ⟺ `is_shefmontazh`).
- W10 предлог «от стоимости» везде — намеренное расхождение с эталонами рама/шеф («за»).
- **W3 (слово «Оплата» для единичного платежа) — ТОЛЬКО внутри `split_by_items`.**
  `prepay_100`/`v3` не трогать — совпадают с эталонами как есть. (Подтверждено S3.)
- Сжатие КП: слияние равных %, срок убран из КП, события коротко.
- Внутренние ключи prepay/postpay/preship — не переименовывать.
- Расщепление бандла ОРИОН — НЕ в F1, отдельный фронт. Опоры (W9) — другое, уже размещены.

**Крупные фронты потом:**
- Расщепление бандла ОРИОН в пути оплаты (после F1, по отдельной разведке).
- Вынос глюкода `2_Договор.py` → `generate_service.py` (под golden-сетью, после bless).
- Количество автовесов > 1 (~7 точек в КП). Деплой → Streamlit Cloud. Реквизиты из файла.
  Админ-панель Фаза 2. Tender-модуль. Миграция FastAPI + React.

**Урок цикла (durable):** «баг на живом файле» многократно оказывался stale-снапшотом или
протухшим session_state — проверять на СВЕЖЕсобранных файлах/чистой сессии, диагноз фактами
(репро-прогон билдеров) ДО фиксов. Прогон через продакшн-билдеры (не LLM-сочинение) —
рабочий метод сверки перед bless: ловит реальные регрессы (S3/W4), не путает намеренные
решения с багами (S3/W3 едва не «починили» зря — отчёт сам поставил оговорку, решение сверено).

---

## Autoverify-харнесс (tests/autoverify) — статус

Построен, зелёный, рабочая сеть для фиксов генерации. Ловушки под F1 добавлены (Стадия 2).
L3-ловушка (W1) осознанно ассертит flat-строку — при реализации W1 перевести на целевую форму.
- [ ] **Шаг 7 (Антон): bless golden + expected_clauses.yaml** — делать после S5-S9 + фиксов +
  ревью. Технически разблокирован (session_state-баг (b) golden не касается), но по-хорошему
  ждём завершения сверки, чтобы заморозить проверенный по всем 9 эталонам вывод.
- [ ] Вынос глюкода в generate_service.py (после bless, под сетью).

---

## Технический долг

### Оплата / формулировки (F1)
- [ ] **W4 supply_filler.py:295** — `.capitalize()` не убран в строке общей стоимости
  договора поставки (регресс из S3, подтверждён). Фикс тривиальный, не применён — в очереди
  на пакетный фикс вместе с находками S5-S9.
- [ ] **Баг (b) — session_state на 2_Договор.py**: протухший `data_editor` при смене КП в
  одной вкладке молча берёт позиции предыдущего КП. Кандидаты: (a) проглоченное исключение
  `build_specification_items` (2_Договор.py:513-520, только WARNING); (b) `key="spec_items_editor"`
  не сбрасывается при загрузке нового КП (805-850). Фикс: сброс ключа в блоке 500-521.
  Интерактивный слой, риск задеть соседнее — opus. Autoverify структурно не ловит.
- [ ] **Два семейства id в матчинге оплаты**: canonical spec_items (`rama`, `pandus`,
  `orion_poles`) vs ключи опций КП. `wording_flags` матчит оба — проверять при новых позициях.
- [ ] **W1** — составная строка «Весы и доставка». Отдельный фронт: расщепить показ-%/расчёт-
  суммы в PaymentLine, перевести L3-ловушку на целевую форму. Opus, под сеткой после bless.
- [ ] **Легаси бандл ОРИОН в пути оплаты** — не расщеплён (риск сумм). Отдельный фронт.
- [ ] Ручные суммы-копейки в редакторе оплаты — режим поверх B.
- [ ] P2: `payment_lines_editor` группирует % по тексту «Объект» → стабильный bucket/source id.

### Баги генерации — техдолг
- [ ] **E2 нумерация размазана**: клаузы нумеруются с 4 в коде, «2.»/«3.» литералами в шаблоне.
- [ ] **C4 привязка хрупкая**: замена «ТПК»→«Компания» привязана к подстроке номера Госреестра.
  ⚠️ T3 сменил номер на `78871-20` — проверить, что привязка не отвалилась молча.
- [ ] **normalize_quotes граница**: под ОДНУ вложенную сущность в кавычках. Имена всегда одна
  сущность (подтверждено) — не задевает реальные данные.

### Валидатор / парсер реквизитов
- [ ] **P1-8** — «Распознать» перетирает ручной ввод (`set_requisites` вместо `merge`).
  Решить: баг или намеренно (`test_dogovor_requisites_reset.py`).
- [ ] **р/с-checksum в валидслое** (средний) — только длина 20 + согласованность к/с↔БИК,
  полная 23-значная контрольная сумма (алгоритм ЦБ 7-1-3) не считается.
- [ ] **P1-7** — валидация ОГРН в парсере (низкий). ⚠️ Мина: эталонный ОГРНИП
  `316619600054321` в `test_requisites_parser.py` битый по контрольной — заменить на
  `304500116000157` перед касанием.
- [ ] P1-6 — статусы «нашёл/не нашёл/не уверен» в UI (низкий).
- [ ] P2-кластер парсера (AUDIT_2026-07): OCR-нормализация, склейка счёта, метка «Адрес:»,
  «факт» адрес → АДРЕС_ПОЧТ, first-wins по двум р/с, `_blank_span`.
- [ ] Natasha для ФИО/адресов — опция, если regex слаб.
- [ ] `_valid_inn` импортируется из парсера в `requisites_validation.py` — не переименовывать.

### Рендеринг / DOCX
- [ ] vMerge ячеек в таблице спецификации DOCX.
- [ ] P3: temp-файл не удаляется при падении `fill_template` в `compose.py`.
- [ ] P3: `merge_runs` guard не защищает `w:pict`/`w:object`/field-runs.
- [ ] `ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА`: константа «не более 50 м».

### Инструменты / скрипты
- [ ] **`make_template.py`** — устаревший путь `SRC` (`03_knowledge_base` → `knowledge_base`).
  Однострочный фикс. ⚠️ Проверить, не в пайплайне bless (тогда чинить раньше).

### Косметика clauses
- [ ] `section_number` в `clauses.yaml` рендерером не используется.
- [ ] `winter_concrete` пересмотреть после scope-редизайна.

### Из ревью v2.2
- [ ] P3: `docs/supply_payment_preview.txt` → пересохранить UTF-8.

### Тесты
- [ ] 4-5 падающих `test_e2e_synthetic`/`case_*` фикстуры (устарели) — `git restore`,
  не коммитить вслепую, разобрать в контрактном треке.

### Крупные рефакторинги
- [ ] `data_loader`: дублирование `equipment_specs` ↔ `models.json`.
- [ ] Терминология `ЗАКАЗЧИК_*` → `КОНТРАГЕНТ_*`.
- [ ] AI-extractor → DeepSeek после стабилизации.
- [ ] Разбить 1019-строчную `2_Договор.py`. Отложено осознанно.

### Фичи (бэклог)
- [ ] Реквизиты из файла (DOCX/XLSX/PDF-текст, без LLM) — spike сначала.
- [ ] Ручная спецификация с загрузкой стройзадания.
- [ ] Реальное удаление позиций прайса через UI (сейчас no-op).
- [ ] Зимний режим фундамента (+обогрев) и «материал заказчика» — проверить охват (см. S8/S9).

### Инфраструктура
- [ ] `use_container_width` → `width=`. ⚠️ Дедлайн 2025-12-31 ПРОШЁЛ.
- [ ] `docs/cloud_claude_handoff_admin_price_ui.md` — untracked, закоммитить или удалить.
- [ ] Supabase temp на Windows: `pytest` без флагов падает `PermissionError`.
- [ ] Сиротские worktree — проверить/удалить.

---

## Админ-панель прайса (PDF-first) — отдельный трек

Фазы 1+1.5 закрыты (`v2.3-admin-phase1`, 92 теста). Фазы 2-5 не начаты — см. предыдущие версии
STATUS/`docs/ADMIN_PANEL_ARCHITECTURE.md` для деталей.

⚠️ Фаза 2 (ценовые правила): КП завязан на числа коэффициентов вилки — нужна регрессия
расчёта КП перед выносом в `pricing_rules.json`.

**Техдолг трека:**
- [ ] EOL `data/prices.json`: LF vs CRLF → `.gitattributes` (`data/prices.json text eol=lf`).
- [ ] `road_slabs`/`pag_slabs`: `price_class=B_retail_only`, но есть `price_dealer_ru`.
- [ ] `price_upload_*` — переоценить после PDF-флоу (не удалять, завязан на `3_Админка.py`).

---

## Известные ограничения (не баги — принятый MVP-скоуп)

- `named_form`/`ИМЕНУЕМ`: Колхоз/Компания/Фирма → «именуемое» (не всегда грамматически точно).
- Между двумя таблицами docx нужен ≥1 абзац-разделитель.
- ОРИОН-приложения (Шаг 10) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- `монолитная_плита`: без стройзадания нет унифицированных чертежей.
- Supply-флоу на docxtpl — отдельная проверка плейсхолдеров при смене шаблонов.
- Автовыбор типа договора — подсказка-дефолт (radio), выбор ручной.

---

## Теги: `v2.2` · `v2.3-admin-phase1` · `v2.4` · (пост-v2.4 в main без тега)

## Команды
```powershell
git status
python -m pytest tests/ --ignore=tests/contracts/synthetic -v
streamlit run src/app.py
```
