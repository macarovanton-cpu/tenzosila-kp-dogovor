# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-06-13

---

## Где мы сейчас

**v2.1 готова. Блок 2 (Договоры) и Блок 3 (Приложения) закрыты полностью.
За эту сессию: P1/P2 баги, переделка редактора оплаты, docxcompose-склейка
(шаг 9), читаемые имена приложений, и модуль ручного ввода реквизитов
с парсером (шаг 11). ОРИОН (шаг 10) вынесен в тех-долг по решению — узкая
специфика, мало сделок. Инструмент готов к внутренней продаже коллегам.**

Последний тег: **v1.2** → пора ставить **v2.1**

---

## Активная задача

v2.1 закрыта. Следующие направления (на выбор, не срочно):
- Тег v2.1 + начало внутреннего тестирования с коллегами
- Деплой на Streamlit Community Cloud
- Разбор тех-долга (CodeGraph MCP, баг имён файлов шага 8)
- Админ-панель прайсов Фаза 2 (была заблокирована до v2.1 — теперь разблокирована)

---

## Что закрыто за эту сессию (2026-06-12 — 2026-06-13)

**P1: Падение редактора оплаты (widget key conflict) — ЗАКРЫТ:**
- `platform_width_m` сидился в session_state И передавался как `default=` в
  segmented_control — Streamlit запрещает. Краш при rerun (триггер —
  взаимодействие с редактором оплаты в Договоре).
- Фикс (вариант A): убран `default=`, убрана мёртвая ветка write-back.

**P2: Оплата не адаптируется (bridge возвращал []) — ЗАКРЫТ:**
- Разобрано по 25 эталонным спецификациям: канонические триггеры PaymentTrigger
  дословно совпадают с формулировками реальных спек.
- bridge расширен на не-split пресеты: v1, v2 (+SHIPMENT_READY), v3
  (DELIVERED/WORK_ACT по trigger_id), prepay_100. custom → [] (корректно).
- База не-split = ИТОГО, share_object = «общей цены договора». Добавлен kind
  «оплата» + share_prep «от».

**Редактор оплаты — переделка «процент истина, сумма производная» (B) — ЗАКРЫТ:**
- Колонка «База, ₽» (видимая; не-split = ИТОГО, split = бакет), «Сумма, ₽» —
  read-only производная.
- Устранён рассинхрон таблица↔предпросмотр (forced rerun, идемпотентный
  recompute, строгие python-типы против бесконечного rerun).
- Валидация: перебор по базе >100% → error; недобор → warning; совпадение →
  caption. Комбинированные split-строки (share_pct=None) не пересчитываются.

**Шаг 9 — docxcompose-склейка — ЗАКРЫТ и подтверждён вручную:**
- compose.py: склейка Спецификация → Приложение №1 (build_task) → №2
  (control_sheet) из contract["attachments"].
- Inline-заглушка удалена из spec_v2.docx, удалён {{APPENDIX_FOUNDATION_CHECK}}.
- **Критический баг merge_runs:** fill_template вызывал `run.text=""` на ранах
  с w:drawing → python-docx clear_content() удалял чертёж → 14 drawing build_task
  превращались в 0 (пустые страницы). Фикс: guard в merge_runs (filler.py).
  Диагностика на уровне XML, гипотеза model_code=None опровергнута DIAG до фикса.
- Разрывы страниц: docxcompose section break не вставляет → явный разрыв перед
  каждым приложением. Без пустых страниц между блоками.

**P2: Читаемые имена приложений — ЗАКРЫТ:**
- pretty_name() + _PRETTY_BASES в fundament_lookup.py
  (`пандусный_С_Ф_3скц.docx` → «Пандусный фундамент 440 мм (С/Ф), 3 секции»).
- Коммит был скрыт generic-сообщением, найден: 6bbb73f.

**Шаг 11 — модуль ручного ввода реквизитов + парсер — ЗАКРЫТ:**
- Цель: снять монополию AI на реквизиты (единая точка отказа при недоступности
  модели). Теперь три пути: парсер копипаста, ручной ввод, AI (для сканов).
- requisites_parser.py: parse_requisites(text) → ЗАКАЗЧИК_*. Формат значения
  первичен, якорь вторичен. ИНН с валидацией контрольной суммы. Конфликты
  КПП/БИК (префикс 04), р/с/к/с (301/407/405/406) разрешены; неоднозначность →
  пусто (не угадывать в юр.документе). ФИО директора — консервативно, только
  при явном одиночном якоре.
- requisites_transforms.py: производные поля. full_name_from_short (^ООО\b,
  не подстрока — ЗАО≠АО), инициалы, должность в РП, склонение ФИО через
  petrovich (+UTF-8-фикс для Windows, uncertain-флаг + warning при несработавшем
  склонении).
- merge_requisites в state.py (патч-обновление без сброса).
- Форма реквизитов вынесена из-под is_extracted() — видна всегда.
- Решение по библиотекам: Natasha/DaData/Pullenti НЕ берём. Коммерческие
  возвращают сетевую зависимость; Natasha не извлекает ИНН/КПП/счета и слабо
  парсит названия. Свой regex по форматам точнее и детерминированнее.
- 695 passed, 2 skipped.

---

## Открытые задачи

### Готовность v2.1
- [ ] Поставить тег v2.1 (после финального smoke реквизитов)
- [ ] Внутреннее тестирование с коллегами
- [ ] codex review модуля реквизитов — отложен (CodeGraph MCP виснет, см. тех-долг)

### Следующие фазы
- [ ] Шаг 10: ОРИОН-приложения (вынесен из v2.1, узкая специфика)
- [ ] Деплой Streamlit Community Cloud
- [ ] Админ-панель прайсов Фаза 2 (разблокирована после v2.1)

---

## Технический долг (не блокирует)

- [ ] **CodeGraph MCP зависает при старте** — `codex review` не проходит,
  замирает на `codegraph/codegraph_status started`. Конфиг codegraph не в
  config.toml репо — искать в глобальном ~/.codex или .codex/ репо. Разобраться
  с таймаутом/переиндексацией. CodeGraph также в workflow для поиска символов —
  чинить всё равно нужно.
- [ ] **Баг шага 8 — имена файлов с пробелом:** `...3 скц.docx` (пробел) в
  библиотеке vs `...3скц.docx` в resolve_build_task → авто-подбор «не найдено».
- [ ] **Natasha для ФИО/адресов** — опция если regex-парсинг ФИО окажется слабым
  на практике. Не сейчас.
- [ ] Ручные суммы-копейки в редакторе оплаты (отдельный режим поверх B)
- [ ] **use_container_width** deprecation → `width=` (до 2025-12-31)
- [ ] **Фича: ручная спецификация с загрузкой строительного задания** —
  переиспользует pdf_to_fundament_docx.py
- [ ] UI-баги страницы Договор: Ctrl+V в «Номер КП»; поиск по Заказчику
- [ ] Баг «Последние КП» не грузит snapshot
- [ ] 5 падающих test_e2e_synthetic
- [ ] AI-extractor → DeepSeek V4 Flash (после стабилизации v2.1)
- [ ] Рефакторинг data_loader: дублирование equipment_specs ↔ models.json
- [ ] vMerge ячеек в таблице спецификации DOCX
- [ ] ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м»
- [ ] tests/storage зависает при сетевых проблемах
- [ ] **Разбор находок аудита 2026-06-13** → docs/audit_2026-06-13.md
  4×P1 парсер реквизитов (адрес/телефон/ФИО/КПП-БИК), 5×P2 платежи,
  2×P3 compose/filler. P1 — первый приоритет, Opus-сессия.
- [x] P1 clauses: `contractor_supervised` — ЗАКРЫТ (dbf0c06): supplier-клоз + customer-клозы стройки.
- [x] P2 clauses: override scope молча игнорируется — ЗАКРЫТ (dbf0c06): visible-UI + guard legacy.
- [x] P2 clauses: зимний чекбокс не гейтится — ЗАКРЫТ (dbf0c06): winter_surcharge_allowed().
- [ ] P2 clauses: ОРИОН-опоры by_contractor при F∉{cf,cwm} — обязательство по опорам выпадает (отложен в тех-долг ОРИОН)

---

## Админ-панель прайсов (параллельный трек)

Фаза 1 закрыта и отревьюена (формат прайса, нормализатор, валидатор, diff,
read-only диагностика).
**Фаза 2 разблокирована** (была заблокирована до закрытия v2.1). AP-008
выполнен: добавлена read-only страница «Админка» с диагностикой текущего
прайса. AP-015 выполнен: добавлена read-only панель правил A/B/C/UNKNOWN/on_request.
AP-010 выполнен: добавлен file-based сценарий validate + diff + download
загруженного JSON-прайса без записи в `data/` и БД. Human review Фазы 2 —
pending; Фазу 3 не начинать без отдельной команды.

---

## Известные ограничения (закрываются позже)

- Обязательства Заказчика: не зависят от scope
- Нумерация Приложений: фиксированная (build_task=1, control_sheet=2), без ОРИОН.
  Подтверждено по 25 спекам — для текущих сделок корректно.
- Контрольный лист: только пандусный и приямок
- монолитная_плита: без строительного задания (нет унифицированных чертежей)

---

## Теги версий

- `v0.6` — стабильный модуль договоров (один шаблон, базовый extractor)
- `v0.7` — Supabase-интеграция, двухрежимный UI
- `v0.8` — закрытие багов после ручной проверки КП→договор
- `v0.9` — массив позиций спецификации + data_editor
- `v1.0` — clauses library + override UI + spec_v2 генерация
- `v1.1` — платёжный редактор + сквозная нумерация спецификации
- `v1.2` — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup шаг 8
- `v2.1` — оплата для всех пресетов + редактор B + docxcompose-склейка +
  читаемые имена приложений + модуль реквизитов (парсер + ручной ввод)
