# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-06-12

---

## Где мы сейчас

**Блок 2 (Договоры) и Блок 3 (Приложения) — шаг 9 (docxcompose-склейка) закрыт
и подтверждён вручную. Все три бага перед шагом 9 закрыты. Склейка
спецификации с приложениями работает: чертежи отображаются, разрывы страниц
корректны. P2 (читаемые имена приложений в UI) закрыт — коммит прошёл, не
потерян. Следующее — шаг 10 (ОРИОН) и шаг 11 (реквизиты Mode A/B) до v2.1.**

**Параллельно:** админ-панель прайсов Фаза 1 закрыта и отревьюена, Фаза 2
заблокирована до закрытия v2.1 (см. секцию «Админ-панель»). Сейчас — v2.1.

Последний тег: **v1.2** (пора ставить v2.1 после шага 10-11)

---

## Активная задача

**Шаг 11: ручной ввод + парсер реквизитов контрагента**

Цель: устранить зависимость от AI для заполнения реквизитов заказчика.
Два новых пути: парсер (копипаст блока) и ручной ввод (форма всегда видима).
AI остаётся третьим путём — не ломаем.

- [x] 11.1 `requirements.txt` — добавить petrovich, установить, проверить API
- [x] 11.2 `src/contracts/requisites_transforms.py` + `tests/contracts/test_requisites_transforms.py`
- [x] 11.3 `src/contracts/requisites_parser.py` + `tests/contracts/test_requisites_parser.py`
- [x] 11.4 `src/contracts/state.py` — `merge_requisites` + тест в `test_state.py`
- [x] 11.5 `src/pages/2_Договор.py` — форма всегда видима, text_area + 2 кнопки
- [x] 11.6 Прогон тестов (695 passed, 2 skipped), коммит ec23089

> Админ-панель прайсов: Фаза 1 закрыта и отревьюена, **Фаза 2 заблокирована до
> закрытия v2.1**. Сейчас админкой не заниматься (см. ниже «Админ-панель»).

---

## Что закрыто за эту сессию (2026-06-12)

**P1: Падение редактора оплаты (widget key conflict) — ЗАКРЫТ:**
- Причина: `platform_width_m` сидился в session_state (state.py) И передавался
  как `default=` в segmented_control — Streamlit это запрещает, отсюда краш при
  rerun (триггерился взаимодействием с редактором оплаты в Договоре).
- Фикс (вариант A): убран `default=` у виджета, убрана мёртвая ветка write-back
  `if state is not st.session_state`. Источник дефолта — сид в state.py.
- Подтверждено: единственный call-site render_model_section — КП-страница со
  st.session_state. 594 passed.

**P2: Оплата не адаптируется (bridge возвращал []) — ЗАКРЫТ:**
- Разобрано по 25 эталонным спецификациям: канонические триггеры PaymentTrigger
  дословно совпадают с формулировками реальных спек. Модель достаточно
  выразительна, расширять не нужно.
- bridge build_lines_from_snapshot расширен на не-split пресеты:
  v1 (SPEC_SIGNED/WORK_ACT), v2 (+SHIPMENT_READY), v3 (DELIVERED/WORK_ACT по
  trigger_id), prepay_100 (SPEC_SIGNED). custom → [] (свободный текст, корректно).
- База не-split строк = ИТОГО спецификации, share_object = «общей цены договора»
  (не привязка к бакетам — это отличие от split_by_items).
- Добавлен kind «оплата» + share_prep «от». Сумма строк добирается остатком на
  последней строке = ИТОГО.

**Редактор оплаты — переделка на «процент истина, сумма производная»
(вариант B) — ЗАКРЫТ:**
- Колонка «База, ₽» (видимая, для не-split = ИТОГО, для split = бакет),
  «Сумма, ₽» — read-only производная (amount = round(pct/100 × base)).
- Устранён рассинхрон таблица↔предпросмотр (forced rerun on change,
  идемпотентный recompute, строгие python-типы против бесконечного rerun).
- Валидация переделана: перебор по базе >100% → error; недобор до ИТОГО →
  warning; точное совпадение → caption. Убран мигающий ruble-error.
- Поймано Opus'ом: комбинированные split-строки (share_pct=None) не
  пересчитываются, сумма из bridge сохраняется.

**Шаг 9 — docxcompose-склейка спецификации с приложениями — ЗАКРЫТ
и подтверждён вручную:**
- Новый модуль src/contracts/compose.py: compose_spec_with_attachments читает
  готовые пути из contract["attachments"], заполняет плейсхолдеры приложений,
  склеивает: Спецификация → Приложение №1 (build_task) → №2 (control_sheet).
- Inline-заглушка приложения удалена из spec_v2.docx (patch_spec_v2_template.py),
  удалён {{APPENDIX_FOUNDATION_CHECK}} и мёртвый код _render_foundation_check.
- Зависимость docxcompose добавлена.
- **Критический баг по ходу — merge_runs уничтожал чертежи:** fill_template
  вызывал `run.text = ""` на ранах с w:drawing → python-docx clear_content()
  удалял drawing → 14 чертежей build_task превращались в 0 (пустые страницы).
  Фикс: guard в merge_runs (filler.py) — не трогать раны с w:drawing.
  Подтверждено: build_task снова 7-8 страниц с чертежами.
- **Разрывы страниц:** docxcompose сам section break не вставляет → каждое
  приложение получает явный разрыв перед склейкой. Приложения начинаются с
  новой страницы, без пустых страниц между блоками.
- Диагностика причины merge_runs — образцовая, на уровне XML (8 PNG, 14 drawing,
  blip refs до/после). Гипотеза model_code=None была опровергнута DIAG-выводом
  до того как чинить — причина доказана, не предположена.

---

## Открытые задачи

### До v2.1
- [x] **P2: Читаемые имена приложений в UI — ЗАКРЫТ.** Маппинг технических имён
  файлов на читаемые реализован: `pretty_name()` + `_PRETTY_BASES` в
  `src/contracts/fundament_lookup.py` (напр. `пандусный_С_Ф_3скц.docx` →
  «Пандусный фундамент 440 мм (С/Ф), 3 секции»). Подключён в
  `src/pages/2_Договор.py` (4 call-site: авто-выбор, опции selectbox, caption
  итогового выбора, caption контрольного листа). Коммит **прошёл, не потерян** —
  `6bbb73f Update foundation model logic and add clauses audit` (предок HEAD).
  Generic-сообщение коммита скрыло его при поиске по «P2/имена».
- [ ] **Шаг 10:** ОРИОН-приложения — библиотека файлов + lookup-логика
- [ ] **Шаг 11:** реквизиты Mode A/B — полное наименование подписантов

После шага 11 — тег v2.1, готовность к внутренней продаже.

---

## Админ-панель прайсов (параллельный трек, ЗАБЛОКИРОВАН до v2.1)

Подготовительная работа выполнена в стороне от v2.1. **Фаза 2 не начата и не
стартует, пока не закрыта v2.1.** AP-008 не запускался, UI админки не делался,
runtime КП/договора не затронут.

**Подготовка (Claude Code):**
- Архитектурный план: `docs/production_architecture_plan.md`.
- Backlog упрощён под solo + AI-агентов: `docs/admin_panel_review_notes.md`,
  `admin_panel_task_breakdown.md`, `admin_panel_agent_rules.md`,
  `admin_panel_status.md`, `tasks/AP-*.md`, `tasks/NEXT_TASK.md`.
- Коммит: `edfe726 Simplify admin panel backlog for solo implementation`.

**Фаза 1 (Codex) — формат прайса, read-only, не подключён к runtime:**
- `2b3a335` docs формата · `122f221` нормализатор · `0b75a82` валидатор ·
  `daf310b` diff версий · `9b93a1a` read-only диагностика.
- Артефакты: `docs/price_format.md`, `src/admin/`, `tests/admin/`.

**Review Фазы 1 (Claude Code):** `40dfc4d` + `docs/admin_panel_phase1_review.md`.
Ошибок не найдено; `src/admin/` изолирован от КП/договора; `tests/admin` →
20 passed; `python -m src.admin.price_diagnostics` → 45 моделей, 65 опций,
0 errors, 4 warnings.

**Правка по review (R1, семантика срока действия):** `301f617 Fix price expiry
semantics` — прайс с `valid_from` без `valid_until` больше не считается expired
(это warning/unknown, не error), чтобы будущий UI не показывал боевой прайс
просроченным.

**Гейт перед Фазой 2:** `tasks/NEXT_TASK.md` указывает на остановку / human
review. Не трогать до закрытия v2.1.

> Git-примечание: вся эта работа на ветке `feature/admin-panel-phase-1`. Работа
> по v2.1 (compose.py, test_compose.py, production_v1.md) возвращена из
> `stash@{0}` поверх неё и продолжается здесь. `stash@{0}` оставлен до коммита
> v2.1, `stash@{1}` не трогать.

---

## Технический долг (не блокирует)

- [ ] **Баг шага 8 — имена файлов с пробелом:** `...3 скц.docx` (пробел) в
  библиотеке vs `...3скц.docx` (без пробела) в resolve_build_task →
  авто-подбор может вернуть «не найдено». Обнаружен при smoke шага 9.
- [ ] Ручные суммы-копейки в редакторе оплаты (отдельный режим поверх варианта B)
- [ ] **Реквизиты подписантов** — полное наименование вместо краткого
- [ ] **use_container_width** deprecation — заменить на `width=` (до 2025-12-31)
- [ ] **Фича: ручная спецификация с загрузкой строительного задания** —
  переиспользует конвертер pdf_to_fundament_docx.py
- [ ] UI-баги страницы Договор: Ctrl+V в «Номер КП»; поиск по Заказчику
- [ ] Баг «Последние КП» не грузит snapshot
- [ ] 5 падающих test_e2e_synthetic
- [ ] AI-extractor → DeepSeek V4 Flash (после стабилизации v2.1)
- [ ] Деплой Streamlit Community Cloud (после v2.1)
- [ ] Рефакторинг data_loader: дублирование equipment_specs ↔ models.json
- [ ] vMerge ячеек в таблице спецификации DOCX
- [ ] ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м»
- [ ] tests/storage зависает при сетевых проблемах

---

## Известные ограничения (закрываются позже)

- Обязательства Заказчика: не зависят от scope
- Нумерация Приложений: фиксированная (build_task=1, control_sheet=2), без учёта
  ОРИОН (шаг 10). Подтверждено по 25 спекам — для текущих сделок корректно.
- Контрольный лист: только пандусный и приямок (для остальных нет чертежей)
- монолитная_плита: без строительного задания (нет унифицированных чертежей)

---

## Теги версий

- `v0.6` — стабильный модуль договоров (один шаблон, базовый extractor)
- `v0.7` — Supabase-интеграция, двухрежимный UI
- `v0.8` — закрытие багов после ручной проверки КП→договор
- `v0.9` — массив позиций спецификации + data_editor
- `v1.0` — clauses library + override UI + spec_v2 генерация
- `v1.1` — платёжный редактор + сквозная нумерация спецификации
- `v1.2` — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup шаг 8
- `v2.1` (готовится) — оплата для всех пресетов + редактор B + docxcompose-склейка