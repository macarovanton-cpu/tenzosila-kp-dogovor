# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-06-27

---

## Где мы сейчас

**Админ-панель прайса (PDF-first), Фаза 1 — ЗАКРЫТА ПОЛНОСТЬЮ.** Тег `v2.3-admin-phase1`.
Весь цикл работает живьём: PDF → таблица → проверка → применение → откат → обновлённый обзор.

**v2.2** — закрыта по коду и протегана; ручной smoke результатов Части 17 висит на Антоне (чеклист ниже).

**Следующее:** Фаза 1.5 (чистка по описи R5) ИЛИ пауза/другой трек. Активного фронта нет.

---

## ⚠️ Висящие ручные проверки (Часть 17 — не подтверждены глазами)

Автотесты зелёные, но юридические документы нельзя выпускать только по тестам.

- [ ] Ручной smoke договора поставки — DOCX, «Покупатель/Поставщик», оплата, нет ссылок на «Спецификацию».
- [ ] Ручной smoke старого spec-флоу (не сломан ли договор-спецификация).
- [ ] Ручной smoke реквизитов (адрес/телефон/директор/КПП-БИК).
- [ ] Ручной smoke платежей (0% по весам, десятичные %, ±1 руб).
- [ ] Binary/layout review DOCX и PDF-фикстур.

---

## Активная задача

**Нет активного фронта.** Фаза 1 админ-панели закрыта. Варианты следующего шага:
- Фаза 1.5 — чистка по описи R5 (см. техдолг трека).
- Ручной smoke Части 17 (висит давно).
- Другой трек / пауза.

⚠️ Нумерация: «Фаза 1» (PDF-first) ≠ старая «Фаза 1/2» из enterprise-карты AP-001…043 (заморожена).
Актуальная карта — docs/ADMIN_PANEL_*.md.

---

## Админ-панель прайса (PDF-first) — Фаза 1 ЗАКРЫТА ✅

Тег `v2.3-admin-phase1`. Все задачи закрыты, 92 admin-теста зелёные, все codex review чистые.

### Backend
- Фаза 0 (разведка R1–R5): схема prices.json, контракты backend, чистый pdfplumber,
  кэш load_prices (@st.cache_data ttl=3600, data_loader.py:30), опись на чистку.
- Задача 1 — фикстуры PDF + заглушки тестов (`6081247`).
- Задача 2 — парсер дилерского PDF (`594f2ce` + `c2fdb5d` опоры 3х/4х). Ключи моделей
  точно совпадают с prices.json, формула ×0.92 на лету отсутствует.
- Задача 3 — парсер розничного PDF (`4c2173b` + 4 P2-фикса). Фундаменты/навесы/услуги/ОРИОН.
  ОРИОН одной суммой, individual_calc. factory_calibration → B (`ed44efb`).
- Задача 4 — merge + дедуп (`f5974b0`). Дилерский приоритет при пересечении опций.
- Задача 5 — запись прайса (`576bb09` + 3 P-фикса). Three-way merge (PDF-снимок +
  ВСЕ JSON-only сохраняются) + backup + atomic + rollback + load_prices.clear().
  Тест имитации сбоя + carryover (exact-equality) + P1-метаданные. Review чистый.

### UI
- Задача 6 — бизнес-сводка изменений (`d5f29bc`).
- Задача 7 — разделение валидации блокеры/предупреждения + блокер пустого ключа (`0411919`).
- Задача 8 — UI таблицы + поток из PDF (`da5d504` + `2f955f5`). Delta-driven сборка
  (ключ распознанной строки неизменяем, служебные поля не теряются). P2-фикс: превью =
  итог three-way merge (preview == apply). Баннер «read-only» исправлен.
- Задача 9 — UI «Редактировать текущий» (`723f893`). Таблица из normalize_prices(load_prices()),
  включая JSON-only позиции.
- Задача 10 — обзор + откат + тех.детали. Бизнес-карточка вверху, технику в expander.
  P2-фиксы: откат доступен при битом prices.json; стейт update-потока сбрасывается при откате.

### Проверено живьём (финальный smoke)
- Блокер при нулевой цене → применение disabled ✅
- Применение пишет prices.json + backup ✅
- Кэш сбрасывается (тест-позиция видна в обзоре сразу после применения) ✅
- Откат возвращает прайс (git diff пустой) ✅
- prices.backup.json в gitignore (`65c3ec5`) ✅

### Принятые решения трека (не пересматривать)
- Дилерская цена — из PDF напрямую, не формулой (п.1a).
- Опоры 3х/4х: 3х выведен с 2026, только 4х (п.1b).
- ОРИОН — одной суммой, не разделять; справочная цена, расчёт индивидуален (п.1c).
- Источник правды — prices.json; правка через единый поток → проверка → diff → бэкап → запись.
- Запись консервативна: JSON-only (монтаж/доставка/поверка/бытовка, road_slabs/pag_slabs,
  vesta-п-80-*) сохраняются всегда. covered-агрегаты: components/ценозависимые notes
  дропаются при изменении итога.
- Удаление позиций через UI пока no-op (three-way merge переносит всё) — реальное удаление
  отложено (отдельная правка write-service, если понадобится).
- Scope: автовесы ВЕСТА + опции/услуги/навесы/ОРИОН. М1/М2 и прочее — вне scope.

---

## Часть 17 — что закрыто (Fable-аудит + стабилизация + договор поставки)

Аудиты Fable (clauses.yaml) + Codex (платежи/compose/реквизиты) → fixes_plan_2026-06-13.md, 3 сессии.
- Сессия 1 (P1 реквизиты): ложный адрес из чисел, телефон из счёта, директор/бухгалтер, КПП/БИК-04.
- Сессия 2 (foundation_scope): customer_builds при монтаже; existing_foundation;
  contractor_supervised P1; зимняя надбавка гейтится scope.
- Сессия 3 (P2 платежи): v2_preship=0; предоплата при 0% весов; десятичные %; ±1 руб; базы бакетов.
- Договор поставки: docxtpl-движок, _buyer_context ЗАКАЗЧИК_*→ПОКУПАТЕЛЬ_*, supplier.py, 3 шаблона.
Тесты 767 → 810. codex review применён в «Apply v2.2 review fixes».

---

## Ранее закрыто (сводка v1.2 → v2.2)

Детальный лог — в чатах-«Частях» 6–17 и git. Ключевое: платёжный редактор (вариант B);
docxcompose-склейка (фикс merge_runs/w:drawing); библиотека фундаментов 23 DOCX + lookup (v1.2);
модуль реквизитов (regex-парсер + ручной ввод, petrovich, INN-checksum).

---

## Технический долг

### Из codex review v2.2 (проверить после «Apply v2.2 review fixes»)
- [ ] P2: payment_lines_editor группирует % по тексту «Объект» → стабильный bucket/source id.
- [ ] P2: synthetic e2e baseline не подтверждён (5/5 failing).
- [ ] P3: docs/supply_payment_preview.txt → пересохранить UTF-8.

### Баги
- [ ] Шаг 8: имена файлов с пробелом («...3 скц.docx» vs «...3скц.docx»).
- [ ] UI Договор: Ctrl+V в «Номер КП»; поиск по Заказчику.
- [ ] «Последние КП» не грузит snapshot.
- [ ] 4–5 падающих test_e2e_synthetic (fixtures case_* изменены в дереве, устарели от генератора).
- [ ] tests/storage зависает при сетевых проблемах.

### Рендеринг / DOCX
- [ ] vMerge ячеек в таблице спецификации DOCX.
- [ ] P3: temp-файл не удаляется при падении fill_template в compose.py.
- [ ] P3: merge_runs guard защищает w:drawing, но не w:pict/w:object/field-runs.
- [ ] ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м».

### Косметика clauses
- [ ] section_number в clauses.yaml рендерером не используется.
- [ ] winter_concrete пересмотреть после scope-редизайна.

### Платежи / реквизиты
- [ ] Ручные суммы-копейки в редакторе оплаты — режим поверх B.
- [ ] Natasha для ФИО/адресов — опция, если regex слаб. Не сейчас.

### Крупные рефакторинги
- [ ] data_loader: дублирование equipment_specs ↔ models.json.
- [ ] AI-extractor → DeepSeek после стабилизации.
- [ ] Терминология ЗАКАЗЧИК_* → КОНТРАГЕНТ_* — большая задача, не сейчас.

### Фичи (бэклог)
- [ ] Ручная спецификация с загрузкой стройзадания (переиспользует pdf_to_fundament_docx.py).
- [ ] Реальное удаление позиций прайса через UI (сейчас no-op) — правка write-service, если понадобится.

### Инфраструктура
- [ ] use_container_width → width=. ⚠️ Дедлайн 2025-12-31 ПРОШЁЛ — проверить, не breaking ли.

### Трек админ-панели — Фаза 1.5 (чистка)
- [ ] road_slabs/pag_slabs: price_class=B_retail_only, но есть price_dealer_ru — битая запись.
- [ ] Чистка по описи R5 (tasks/AP-*, старые docs/admin_panel_*, ветки-артефакты
      feature/admin-panel-phase-1 / sunrise-hyacinth / backup-home-gitignore-fix).
- [ ] price_upload_* — переоценить (нужен ли старый JSON-загрузчик после PDF-флоу).

### Решено
- [x] CodeGraph MCP зависал при codex review — обход
      `codex review --commit <SHA> -c 'mcp_servers.codegraph.enabled=false'`. В CLAUDE.md.

---

## Известные ограничения

- ОРИОН-приложения (Шаг 10 договоров) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- монолитная_плита: без стройзадания нет унифицированных чертежей.
- Supply-флоу на втором движке (docxtpl) — при изменениях шаблонов отдельная проверка плейсхолдеров.
- Удаление позиций прайса через UI — пока no-op.

---

## Деплой и горизонт

- Деплой Streamlit Cloud / VPS — после стабилизации.
- FastAPI + React + Bitrix24 (REST) — после закрепления Streamlit.
- Tender-модуль (RAG по архиву ТЗ) — после договоров.

---

## Теги версий

- v1.2 — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup Шаг 8.
- v2.1 — оплата для всех пресетов + редактор B + docxcompose + читаемые имена + модуль реквизитов.
- v2.2 — договор поставки + 3 сессии стабилизации + codex review fixes. Ручной smoke — см. чеклист.
- v2.3-admin-phase1 — админ-панель прайса PDF-first: парсеры + merge + запись + UI + откат.

---

## Команды

\```powershell
git status
rtk python -m pytest tests/ -v
streamlit run src/app.py
\```