# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-07-11

Только невыполненное и техдолг. История закрытых задач — в git log.

---

## Активная задача

**Нет открытого фронта.** Все фронты сессии 2026-07-10/11 закрыты и в `main`.

**Следующее по плану:** уборка репозитория (см. ниже) → деплой на Streamlit Cloud.

---

## Закрыто в сессии 2026-07-10/11 (в `main`)

- **F1 — единый источник формулировок оплаты.** Свод двух генераторов (КП lite /
  Спецификация full) в `src/payment_wording.py`. Сверка с 9 эталонами через
  продакшн-билдеры — 0 регрессов (`REVIEW_scenarios_2026-07-10.md`). W4-фикс
  (`.capitalize()` в `supply_filler.py:295`) применён.
  **Заморозка F1 = inline-ловушки autoverify + сверка 9 эталонов.** Отдельного
  golden-snapshot-механизма в коде НЕТ и не строили (см. техдолг).
- **S6 — «опоры ОРИОН без фундамента»**: UX-митигация вместо правки бакет-логики.
  `st.warning` + `st.expander` с рекомендуемой формулировкой (согласовать с юристом).
  Достижимо из UI (опоры — независимый чекбокс). Бакет-логику не трогали.
- **Баг (b) — протухшие позиции при смене КП.** Диагноз: НЕ widget-state `data_editor`
  (опровергнуто исходником Streamlit 1.55). Три реальные причины: не-атомарный коммит
  state, elif-тень ручного поиска, кэш `_load_kp_full` без версии. Фикс: атомарная
  `build_kp_payload` (`src/contracts/kp_load.py`), disable «Найти» при активном
  дропдауне, `updated_at` в кэш-ключе.
- **Количество автовесов > 1 (qty).** DOCX-генерация уже была ×N корректна везде.
  Чинили: `calc_totals` ×model_qty (UI-итоги + `total_price` в Supabase), количество
  убрано из заголовков supply-договора (возврат к эталону, включая qty=1).
  Инвариант КП=договор при qty>1 закрыт тестом в autoverify.
- **A2/P1-8 — «Распознать» перетирало ручной ввод.** UI обещал «уже заполненное не
  затирается», код перезаписывал. Фикс: отслеживание ручного ввода (`requisites_manual`),
  `merge_requisites` пропускает помеченные поля; производные (ФИО РП, инициалы) следуют
  за актуальным родителем; «Очистить реквизиты» сбрасывает метки. Пол директора защищён
  той же семантикой.
- **A3 — `use_container_width` → `width="stretch"`** (3 × `st.data_editor`). Гейт деплоя снят.
- **Вёрстка «Реквизиты заказчика»**: построчная раскладка, 10 пар без пустых ячеек,
  банковские пары по смыслу, «Пол директора» в блоке руководителя.
- **UI-полировка страницы Договор** (раздел C): «Тип документа» перенесён внутрь секции
  «Параметры договора»; ошибки реквизитов перенесены в сайдбар (унифицировано с
  КП-страницей); пункт «app» убран из навигации.

**Закрыты как ложная тревога (записи в техдолге были устаревшими):**
- A1 / C4 (привязка «ТПК»→«Компания» к номеру Госреестра) — маркер уже `78871-20`, тест зелёный.
- B3 (vMerge в таблице КП) — баг не воспроизводится, починен ранее.
- Монтаж-drop (КП № 223311) — оказался протухшим session_state, не багом билдеров.

---

## Очередь

1. **Уборка репозитория** — инвентаризация и чистка отработанных рабочих документов
   (RECON_*, PLAN_*, FIX_SPEC_*, REVIEW_*), скриншотов, временных артефактов.
2. **Деплой → Streamlit Cloud** (secrets, Supabase, версии).
3. Далее по факту обратной связи от менеджеров.

---

## Autoverify-харнесс (tests/autoverify)

Построен, зелёный, рабочая сеть для фиксов генерации. Ловушки под F1 добавлены.
L3-ловушка (W1) осознанно ассертит flat-строку — при реализации W1 перевести на целевую форму.
Фикстуры qty=2 (`*_x2.json`) + парсинг «ИТОГО за N весов» — инвариант КП=договор при qty>1.

Заморозка F1 = inline-ловушки через продакшн-билдеры + сверка 9 эталонов. Отдельного
golden-snapshot-файла НЕТ.

---

## Технический долг

⚠️ **Приоритизированный техдолг — `docs/TECH_DEBT.md`** (разделы A/B/C). Этот раздел —
только то, что не вошло туда.

⚠️ **Урок:** записи в техдолге устаревают. Из проверенных пунктов **три оказались уже
закрытыми** (A1, B3, монтаж-drop), один содержал неверную ссылку на код (A2 указывал на
несуществующий `set_requisites` вместо `merge_requisites`). **Перед фиксом любого пункта —
сначала проверка «а он вообще жив?», диагноз фактами на свежесобранных файлах.**

### Оплата / формулировки
- [ ] **Golden-snapshot-механизм для autoverify** — `generate_all()` → сериализация → diff.
  Prerequisite выноса глюкода в `generate_service.py`. Дизайн: что морозить + судьба
  in-memory сценариев S3/S5/S8 (сейчас не JSON-фикстуры). Сейчас заморозка на inline-ловушках.
- [ ] **W1** — составная строка «Весы и доставка» остаётся flat (осознанно, не баг).
  Расщепление показ-%/расчёт-суммы в `PaymentLine` — риск для инварианта сумм. Opus, под сеткой.
- [ ] **Легаси бандл ОРИОН в пути оплаты** — не расщеплён (риск сумм). Отдельный фронт.
- [ ] **Два семейства id в матчинге оплаты** — canonical spec_items vs ключи опций КП.
  `wording_flags` матчит оба — проверять при добавлении новых позиций.
- [ ] **`resolve_payment_group` для `orion_cable_poles`** — опоры в бакете `foundation` без
  проверки наличия физического фундамента. Прикрыто UX-предупреждением (S6), правка
  бакет-логики отложена намеренно.
- [ ] Ручные суммы-копейки в редакторе оплаты. P2: `payment_lines_editor` группирует % по
  тексту «Объект» → нужен стабильный bucket/source id.

### Баги генерации
- [ ] **Дефект 3 (латентный, qty>1)**: `from_kp.py:300` `grand_total` per-unit без ×model_qty.
  Маскирован (`spec_v2` перезаписывает), помечен комментарием. Чинить, если пойдёт в расчёт.
- [ ] E2 нумерация клауз размазана (с 4 в коде, «2.»/«3.» литералами в шаблоне).
- [ ] `normalize_quotes` — граница под одну вложенную сущность (реальные данные не задевает).
- [ ] `ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА`: константа «не более 50 м».

### Прочее
См. `docs/TECH_DEBT.md` — валидатор/парсер реквизитов, рендеринг DOCX, крупные рефакторинги,
фичи-бэклог, инфраструктура, админ-панель.

---

## Известные ограничения (не баги — принятый MVP-скоуп)

- `named_form`/`ИМЕНУЕМ`: Колхоз/Компания/Фирма → «именуемое» (не всегда грамматически точно).
- Между двумя таблицами docx нужен ≥1 абзац-разделитель.
- ОРИОН-приложения (Шаг 10) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- `монолитная_плита`: без стройзадания нет унифицированных чертежей.
- Supply-флоу на docxtpl — отдельная проверка плейсхолдеров при смене шаблонов.
- Автовыбор типа договора — подсказка-дефолт (radio), выбор ручной.

---

## Теги: `v2.2` · `v2.3-admin-phase1` · `v2.4` · (пост-v2.4 в main без тега)

## Команды
```powershell
git status
python -m pytest tests/ --ignore=tests/contracts/synthetic -v
streamlit run src/app.py
```
