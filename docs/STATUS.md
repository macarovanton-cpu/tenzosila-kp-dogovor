# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-07-09

Только невыполненное и техдолг. История закрытых задач — в git log.

---

## Активная задача

**Консолидированный фронт багов генерации спецификаций — почти закрыт.**
Метод: построчная сверка сгенерированных спецификаций с эталонами
`docs/contract_templates_v1/*.md` по 5 сценариям (Ромашка, Агрофирма×3, ТАТПРОФ×2,
ВКМ) + autoverify-ловушки на каждый баг (красные = баг воспроизведён, зелёные = починен).

Рабочие документы (в docs/): `FIX_SPEC_generation_2026-07-08.md` (спецификация фиксов:
как сейчас → как должно → критерий), `DIAG_P0_2026-07-08.md` (диагностика корней),
`BUGS_generation_2026-07-08.md` (исходный баг-лист).

**Пройдено (заходы 1-4):**
- Заход 1: ловушки на все фиксы (35 красных). ✓
- Заход 2: БАГ-1a (рама→фундамент: рассинхрон `frame_`≠`rama` в spec_items.py),
  E4 (пандусы `ramp_set_`→custom), часть БАГ-1b. Коммит `f8a659c`. ✓
- Заход 3: C1+C3 (кавычки заказчика→ёлочки), C4 («Компания «Тензосила» в Госреестре),
  D2+B1 (выезд бригад по признаку стройки, без дублей), D3 («Стороны» ×1),
  E1 (точка адреса), E2 (нумерация 2./3.), E3a (порядок позиций), E3b (наименования
  по эталону — override в from_kp.py). 8 коммитов. opus-ревью чистое. ✓
- Заход 4: A1 (расщепление ОРИОН-пакета: ПАК + монтаж + опоры вместо 1 строки).
  Коммиты `5dd8a42`, `e5cb08b`. Расщепление ПАК/монтаж — верно. ✓

**ОТКРЫТО — фикс захода 4 (единственный незакрытый фронт):**
Заход 4 ошибочно сделал опоры ОРИОН **авто-добавляемыми в договор** (корень — неверная
формулировка в FIX_SPEC «опоры авто-добавляются»). Правило Антона: **опоры — обычная
опция, менеджер включает в КП явно; в договор попадают ТОЛЬКО из КП.** Последствия бага:
опоры появлялись в договоре без основания в КП; КП/договор разошлись на 155к; `test_arithmetic`
ослаблен костылём `metadata.auto_added`.
**Что делать (промт готов):** откатить авто-добавление опор (опоры текут из снапшота КП
как обычная опция); убрать костыль auto_added; восстановить инвариант «сумма КП = сумма
договора». Расщепление ПАК/монтаж НЕ трогать (верно). Развилка в плане: опция опор
`orion_cable_poles` уже доступна в UI/КП, или нужна UI-правка (тогда СТОП, отдельный заход).
FIX_SPEC уже исправлен (правило «опоры из КП, не авто»).

**Осталось после фикса захода 4:**
- Заход 5 — **F1 (умный дефолт оплаты по вехам).** Отдельный фронт, opus-plan.
  Генератор сеет вехи по бакетам (весы/фундамент/монтаж, %% из эталонов) вместо
  плоского 30/40/30; менеджер правит вручную. Проценты (50/60) выверить с Антоном ДО кода.
  Пересекается с легаси-путём оплаты v1 (заход 4 оставил хвост: render_payment_block
  бандл не делит, перекрывается редактором оплаты).
- Смысловая сверка формулировок с эталонами — делает Claude Code по запросу (не код, не API).

**Снято в ходе разбора (не баги):**
- D1 (дубль Госреестра) — не воспроизводится на коде, stale-артефакт/ручная правка живого файла.
- БАГ-1b «мёртвая константа existing_foundation» — константа ЖИВАЯ (путь scope_overrides,
  UI «Готовый фундамент Заказчика», покрыта test_supply_filler.py:299).
- ОРИОН «выпадал» на ВКМ — был stale-снапшот (загрузка КП по номеру до маппинга ОРИОН);
  на свежесобранном ВКМ ОРИОН доходит.

**Урок цикла:** «баг на живом файле» трижды оказывался stale-артефактом или ручной
правкой (ОРИОН, D1, конструкция КП). Проверять баги только на СВЕЖЕсобранных файлах.
Плюс: доменный контроль Антона глазами поймал ошибку в самих требованиях (авто-опоры) —
агент реализовал по спеке, спека была неверна. Решения — Антона, реализация — агента.

**Очередь после багов генерации:**
1. Autoverify шаг 7 (Антон): bless golden-дампов + карта клауз `expected_clauses.yaml`
   (после того как все фиксы влиты — заморозить чистый выход).
2. Вынос глюкода `2_Договор.py:959-1010` → `src/contracts/generate_service.py`
   (страница и runner зовут одно; сейчас runner — ЗЕРКАЛО, парные маркеры). ПОД golden-сетью,
   после bless. Opus + codex review.
3. Количество автовесов > 1 — механическая доработка КП (~7 точек). Не блокер пилота.
4. Деплой → Streamlit Cloud (секреты/env, домен, лимиты).
5. UI/UX аудит (Playwright) — затрагивает страницу реквизитов.

**Крупные фронты потом:**
- Реквизиты из файла (DOCX/XLSX/PDF-текст) — feasibility-spike, см. техдолг → «Фичи».
- Админ-панель Фаза 2. Tender-модуль.
- Миграция FastAPI + React (фаза 0: golden dump P0-01 готов; P0-02 фикстуры,
  P0-03 заморозка). Autoverify ляжет как регресс бэкенда.

---

## Autoverify-харнесс (tests/autoverify) — статус

Построен, зелёный, стал рабочей сетью для фиксов генерации:
- Раннер (headless-зеркало глюкода 2_Договор.py, парные маркеры «ЗЕРКАЛО»),
  docx_text.py, фикстуры (rama_montazh, fundament_montazh_poverka, orion, winter_concrete,
  + добавлены заходом 1: rama_ploshadka_montazh [frame_* — тестирует БАГ-1a/E4],
  montazh_orion, rama_bez_montazha [supply]).
- Ловушки на баги генерации (группы C/D/E/A1) — по мере фиксов зеленеют.
- Гейт достоверности раннера ПРОЙДЕН (сверка UI vs runner на rama_montazh; расхождение
  было в attachment-шаге — починено; остальное — несовпадение входа/stale, не баг раннера).
- [ ] Шаг 7 (Антон, ПОСЛЕ всех фиксов): bless golden + expected_clauses.yaml.
- [ ] Вынос глюкода в generate_service.py (после bless, под сетью).

---

## Технический долг

### Баги генерации — техдолг после фиксов (заходы 2-4)
- [ ] **E2 нумерация размазана**: клаузы нумеруются с 4 в коде (clauses_renderer.py),
  «2.»/«3.» вбиты литералами в шаблон. Работает, но при смене старта нумерации в коде —
  рассинхрон. Свести к одному источнику — не сейчас.
- [ ] **C4 привязка хрупкая**: замена «ТПК»→«Компания» в Госреестре привязана к подстроке
  `78871-20`. При смене номера Госреестра отвалится молча. Лучше отдельный плейсхолдер — потом.
- [ ] **normalize_quotes граница**: специализирован под ОДНУ (вложенную) сущность в кавычках.
  На двух независимых парах («ООО "ТД "Х" плюс») или тексте после закрывающей — молчаливый
  брак; одиночная кавычка теряется. Антон подтвердил: имена всегда одна сущность — граница
  не задевает реальные данные. Доработать, если появится нетиповое имя.
- [ ] **Легаси-путь оплаты v1** (render_payment_block) бандл ОРИОН не делит — перекрывается
  редактором оплаты (test_payment_rows_cover_total зелёный). Окончательно решится в F1 (заход 5).

### Валидатор / парсер реквизитов
- [ ] **P1-8 — «Распознать» перетирает ручной ввод.** UI зовёт `set_requisites` (полная
  замена), не `merge`. Сначала решить: баг или намеренно (есть `test_dogovor_requisites_reset.py`).
- [ ] **р/с-checksum в валидслое** (средний). Сейчас только длина 20 + согласованность к/с↔БИК.
  Полная 23-значная контрольная сумма р/с не считается. Функция дешёвая, алгоритм известен.
- [ ] **P1-7 — валидация ОГРН в парсере** (низкий). Валидслой уже проверяет (`_valid_ogrn`).
  ⚠️ **Мина:** эталонный ОГРНИП `316619600054321` в `test_requisites_parser.py` битый по
  контрольной — тест жив, пока парсер ОГРН не валидирует. Перед касанием P1-7 заменить
  на валидный (напр. `304500116000157`).
- [ ] **P1-6 — статусы «нашёл/не нашёл/не уверен» в UI** (низкий). Острие снято валидслоем.
- [ ] **P2-кластер парсера** (AUDIT_2026-07): OCR-нормализация (О→0/З→3/l→1) + склейка счёта
  с пробелами; метка «Адрес:» не срезается; «факт» адрес → АДРЕС_ПОЧТ; first-wins по двум р/с;
  `_blank_span` может съесть кавычки имени, если банк раньше названия.
- [ ] Natasha для ФИО/адресов — опция, если regex слаб. Не сейчас.
- [ ] `_valid_inn` импортируется из парсера в `requisites_validation.py` — **не
  переименовывать** при рефакторинге (молча сломает валидслой на импорте).

### Рендеринг / DOCX
- [ ] vMerge ячеек в таблице спецификации DOCX.
- [ ] P3: temp-файл не удаляется при падении `fill_template` в `compose.py`.
- [ ] P3: `merge_runs` guard защищает `w:drawing`, но не `w:pict`/`w:object`/field-runs.
- [ ] `ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА`: константа «не более 50 м».

### Косметика clauses
- [ ] `section_number` в `clauses.yaml` рендерером не используется.
- [ ] `winter_concrete` пересмотреть после scope-редизайна.

### Платежи
- [ ] Ручные суммы-копейки в редакторе оплаты — режим поверх B.
- [ ] P2: `payment_lines_editor` группирует % по тексту «Объект» → стабильный bucket/source id.
  (пересекается с F1 — заход 5).

### Из codex review v2.2
- [ ] P3: `docs/supply_payment_preview.txt` → пересохранить UTF-8.

### Тесты
- [ ] 4-5 падающих `test_e2e_synthetic` / `case_*` фикстуры (устарели от генератора) —
  всплывают в `git status` как modified. `git restore`, не коммитить вслепую; разобрать
  в контрактном треке.

### Крупные рефакторинги
- [ ] `data_loader`: дублирование `equipment_specs` ↔ `models.json`.
- [ ] Терминология `ЗАКАЗЧИК_*` → `КОНТРАГЕНТ_*` — большая задача, не сейчас.
- [ ] AI-extractor → DeepSeek после стабилизации.
- [ ] P2 из архитектурного аудита: разбить 1019-строчную `2_Договор.py`. Отложено осознанно.

### Фичи (бэклог)
- [ ] **Реквизиты из файла** (DOCX/XLSX/PDF-текст, без LLM) — адаптер `extract_text→parse_requisites`.
  DOCX/XLSX легко, PDF-текст средне, `.doc`/сканы вне scope (сканы → vision-LLM, Tender).
  Первый шаг — spike: 5-6 файлов, замер % hard-полей, БЕЗ UI. Детект «нет текстослоя» громко.
- [ ] Ручная спецификация с загрузкой стройзадания (переиспользует `pdf_to_fundament_docx.py`).
- [ ] Реальное удаление позиций прайса через UI (сейчас no-op).
- [ ] Зимний режим фундамента (+обогрев) и «материал заказчика» — проверить охват.

### Инфраструктура
- [ ] `use_container_width` → `width=`. ⚠️ Дедлайн 2025-12-31 ПРОШЁЛ — проверить.
- [ ] `docs/cloud_claude_handoff_admin_price_ui.md` — untracked, закоммитить или удалить.
- [ ] Supabase temp на Windows: `pytest` без флагов падает `PermissionError` к
  `C:\Users\tonik\AppData\Local\Temp\pytest-of-tonik`. Удалить каталог руками.
- [ ] Сиротские worktree `.claude/worktrees/recursing-boyd-31e597` +
  `.kilo/worktrees/sunrise-hyacinth` — проверить/удалить.

---

## Админ-панель прайса (PDF-first) — отдельный трек

Фазы 1 + 1.5 закрыты (`v2.3-admin-phase1`, 92 теста). Фазы 2-5 не начаты:
- **Фаза 2 — Ценовые правила.** Коэффициенты вилки `config.py` → `data/pricing_rules.json`.
  ⚠️ КП завязан на числа коэффициентов — нужна регрессия расчёта.
- **Фаза 3 — Диагностика.** Подсветка аномалий, предупреждение о просроченном прайсе.
- **Фаза 4 — UX-полировка.**
- **Фаза 5 — Регрессия + тег.**
Карта: `docs/ADMIN_PANEL_ARCHITECTURE.md` / `_PHASES_TASKS.md` / `_REVIEW_CHECKLIST.md`.

**Техдолг трека:**
- [ ] EOL `data/prices.json`: `write_prices` пишет LF, репо CRLF → фантомно-modified.
  Лечится `.gitattributes` (`data/prices.json text eol=lf`).
- [ ] `road_slabs`/`pag_slabs`: `price_class=B_retail_only`, но есть `price_dealer_ru`.
- [ ] `price_upload_*` — переоценить после PDF-флоу (не удалять, завязан на `3_Админка.py`).

---

## Известные ограничения (не баги — принятый MVP-скоуп)

- `named_form`/`ИМЕНУЕМ`: ИП vs ООО/АО/ПАО/ЗАО/НАО покрыты; Колхоз/Компания/Фирма →
  «именуемое» (не всегда грамматически точно).
- Между двумя таблицами docx нужен ≥1 абзац-разделитель, иначе Word сливает.
- ОРИОН-приложения (Шаг 10) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- `монолитная_плита`: без стройзадания нет унифицированных чертежей.
- Supply-флоу на docxtpl — при изменениях шаблонов отдельная проверка плейсхолдеров/колонтитулов.
- Автовыбор типа договора — подсказка-дефолт (radio), выбор ручной (закрыто заходами 2-3).

---

## Теги: `v2.2` · `v2.3-admin-phase1` · `v2.4` · (пост-v2.4 в main без тега)

## Команды
```powershell
git status
python -m pytest tests/ --ignore=tests/contracts/synthetic -v
streamlit run src/app.py
```
