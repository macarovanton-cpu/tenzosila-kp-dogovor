# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-06-10

---

## Где мы сейчас

**Блок 2 (Договоры) — шаги 1–9 закрыты. Блок 3 (Приложения) — lookup + UI выбора
фундаментных приложений готовы, docxcompose-склейка спецификации с приложениями
готова. Следующий шаг — 10 (ОРИОН-приложения). Три открытых бага остаются.**

Последний тег: **v1.2**

---

## Активная задача

Шаг 9 закрыт (8795cca). Следующая активная задача — вернуться к открытым багам:
1. **Читаемые имена приложений в UI** (P2) — технические имена файлов
   (`пандусный_С_Ф_3скц.docx`) вместо понятных менеджеру формулировок
2. **Оплата «по умолчанию» не адаптируется** (P2) — bridge v2/v3/prepay
   возвращает [], DOCX берёт КП-рендерер без адаптации под стиль спецификаций
3. **Падение редактора оплаты** (P1) — widget key conflict `platform_width_m`
   на странице КП при взаимодействии с редактором оплаты в договоре;
   также в логе: `build_specification_from_kp_snapshot: 6 строк > 5 слотов шаблона`

---

## Что закрыто за последние сессии

**Блок 2 — Договоры, шаги 1–7:**
- Шаг 1a: PaymentLine + format_payment_line + пропись
- Шаг 1b: build_lines_from_snapshot — bridge split_by_items, P1/P2 баги закрыты
- Шаг 3: бакеты + data_editor + payment_group в from_kp
- Шаг 4: платёжный редактор UI — payment_lines_editor.py, state.py, 9 тестов
- Шаг 5: сквозная плоская нумерация секций — убраны заголовки-подсекции,
  плейсхолдеры {{ТТХ_НОМЕР}}/{{КОМПЛЕКТ_НОМЕР}} патчем шаблона,
  obligations_range пересчитан, тесты обновлены (ffae2fc)
- Шаг 6: сроки + зимний вариант — terms_renderer.py, температурное условие,
  ссылки на п.2.1/п.2.2, отправка бригады, числа прописью
- Шаг 7: обнулён — contractor_supervised убран (единичные сделки, не паттерн),
  base_type откладывается в Блок 3 (влияет на чертежи, не на клаузы),
  юр-ревью clauses.yaml отложено

**Блок 3 — Библиотека фундаментов (полностью собрана):**
- Конвертер PDF→DOCX написан с нуля: scripts/pdf_to_fundament_docx.py + core
- Оформление по эталону templates/contracts/spec_v2.docx:
  header, шапка приложения, TextBox «Утверждаю {{ЗАКАЗЧИК_ДИРЕКТОР_ИНИЦИАЛЫ}}»
  на каждой странице, pageBreakBefore убран (добавится при склейке)
- Пакетный режим: --all обрабатывает pdf_source/ + control_sheet/ подпапку
- Состав библиотеки (18 build_task + 5 control_sheet = 23 DOCX):
  build_task:
    пандусный_С_Ф × 3, пандусный_СЛ_ФЛ × 3
    (350мм = СЛ/ФЛ, 440мм = С/Ф/П — менеджеры ориентируются по мм)
    приямок × 3, rama_concrete × 3
    rama_road_slabs × 3, rama_pag_slabs × 3
  control_sheet:
    пандусный × 3 (2/3/4 скц), приямок × 2 (3/4 скц — 2скц не существует)

**Шаг 8 — lookup + UI выбора приложений:**
- src/contracts/fundament_lookup.py:
  resolve_build_task(snapshot) → Path | None + reason
  resolve_control_sheet(execution, sections) → Path | None
- Family из model_code: С/Ф/П → С_Ф, СЛ/ФЛ → СЛ_ФЛ
- монолитная_плита и None → без приложения (валидный случай, не ошибка)
- UI в 2_Договор.py: автоподбор + selectbox override + чекбокс контрольного листа
  (чекбокс только для пандусный/приямок когда файл существует)
- Хранение в contract["attachments"], склейка — шаг 9
- 594 passed, 1 skipped

**Шаг 9 — docxcompose-склейка приложений:**
- Новый src/contracts/compose.py подклеивает приложения к Спецификации на месте:
  build_task → Приложение №1, control_sheet → Приложение №2
- Внешние приложения перед склейкой заполняются через fill_template; сырые {{...}}
  проверяются уже после склейки
- У первого параграфа приложения снимается pageBreakBefore, чтобы не было пустой
  страницы после section break от docxcompose
- Инлайн-заглушка приложения удалена из templates/contracts/spec_v2.docx
- Конвертер PDF→DOCX теперь берёт эталон оформления из внешнего build_task DOCX,
  а не из spec_v2.docx
- 627 passed, 1 skipped

**Попутные фиксы:**
- P2-баг ТТХ_ГРАНИЦА — закрыт как фантомный (код корректен)
- Форматирование строки «Дискретность» в таблице ТТХ исправлено в шаблоне
- pytest.ini — tests/e2e в постоянном ignore (playwright недоступен)
- git identity настроен на рабочей машине (DLAdmin2024 → Anton Makarov)

---

## Открытые задачи

### Текущие баги
- [ ] **P1: Падение редактора оплаты** — widget key conflict `platform_width_m`
  при взаимодействии редактора оплаты на странице Договор.
  Traceback: src/ui/model_section.py:121 `_render_platform_width_control`.
  Также: `build_specification_from_kp_snapshot: 6 строк > 5 слотов шаблона`
  в логе — bridge overflow платёжных строк.
- [ ] **P2: Оплата «по умолчанию» без адаптации** — кнопка «Заполнить по умолчанию»
  берёт КП-рендерер без адаптации под стиль спецификаций договора.
  Bridge v2/v3/prepay пресетов возвращает [].
- [ ] **P2: Читаемые имена приложений в UI** — сейчас показываются технические
  имена файлов (`пандусный_С_Ф_3скц.docx`). Нужен маппинг на читаемые:
  «Пандусный фундамент, 3 секции (С/Ф)», «Приямок, 3 секции» и т.д.

### Блок 3 — Приложения (продолжение)
- [x] **Шаг 9:** docxcompose-склейка
  спецификации + строительного задания + контрольного листа
- [ ] **Шаг 10 (следующий):** ОРИОН-приложения
- [ ] Шаг 11: реквизиты Mode A/B

---

## Технический долг (не блокирует)

- [ ] **Реквизиты подписантов** — полное наименование вместо краткого в блоке подписей
- [ ] **install_default / verification_default** — расхождение _SIMPLE_OPTION_NAMES / prices.json
- [ ] **use_container_width** deprecation — заменить на `width=` (до 2025-12-31)
- [ ] **Фича: ручная спецификация с загрузкой строительного задания** — менеджер
  загружает произвольный PDF, получает оформленное DOCX-приложение.
  Переиспользует конвертер pdf_to_fundament_docx.py. Не срочно.
- [ ] UI-баги страницы Договор: Ctrl+V в «Номер КП»; поиск по Заказчику
- [ ] Баг «Последние КП» не грузит snapshot
- [ ] 5 падающих test_e2e_synthetic
- [ ] AI-extractor → DeepSeek V4 Flash (после стабилизации v2.1)
- [ ] Деплой Streamlit Community Cloud (после v2.1)
- [ ] Рефакторинг data_loader: дублирование equipment_specs ↔ models.json
- [ ] vMerge ячеек в таблице спецификации DOCX
- [ ] ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м»
- [ ] tests/storage зависает при сетевых проблемах

---

## Известные ограничения v1.2 (закрываются в v2.1)

- Обязательства Заказчика: не зависят от scope
- Нумерация Приложений: фиксированная, без учёта ОРИОН
- ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м»
- Контрольный лист: только пандусный и приямок (для остальных нет чертежей)
- монолитная_плита: без строительного задания (редкий случай, нет унифицированных чертежей)

---

## Лог

- 2026-06-10 — Шаг 9 закрыт: docxcompose-склейка спецификации с внешними
  приложениями, удалена inline-заглушка spec_v2, pytest зелёный (8795cca).

---

## Теги версий

- `v0.6` — стабильный модуль договоров (один шаблон, базовый extractor)
- `v0.7` — Supabase-интеграция, двухрежимный UI
- `v0.8` — закрытие багов после ручной проверки КП→договор
- `v0.9` — массив позиций спецификации + data_editor
- `v1.0` — clauses library + override UI + spec_v2 генерация
- `v1.1` — платёжный редактор + сквозная нумерация спецификации
- `v1.2` — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup шаг 8
