# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-07-07 (сессия сверки сценариев)

---

## Где мы сейчас

**v2.4 отгружен и запушен.** Поверх него: сессия применения архитектурного аудита
(`AUDIT_2026-07.md`) закрыта, затем открыт фронт **ручной сверки сценариев генерации
договоров** — идёт сейчас. Найдены баги генерации (4 подтверждённых + 2 на проверку),
собран тест-сьют на 8 сценариев. Детали — секция «Сверка сценариев (07-07)» ниже.

**Договор поставки — ЗАКРЫТ.** Пакет фиксов (2 волны) закоммичен, запушен, подтверждён
перепрогоном. Телефон покупателя (`ПОКУПАТЕЛЬ_ТЕЛЕФОН`) — **починен**, проверен вручную.

**«Последние КП» — ОБА бага ЗАКРЫТЫ ✅** (проверено вручную в Streamlit).
- Краш (`1aafd37`): `float('')`/`int('')` на пустых числовых полях старых снапшотов
  ронял страницу. Фикс: 6 точек через `not in (None, "")` (не `or`, чтобы `qty=0` не
  пропадал). Путь загрузки — ручной поиск по номеру (`select("*")`, снапшот есть).
- Тихий пустой договор из дропдауна: диагностика (Opus) опровергла гипотезу
  «`1aafd37` создал» — это **отдельный старый баг**. `list_recent_kps` делал `select`
  БЕЗ колонки `data` (JSONB-снапшот), дропдаун отдавал строку без снапшота → `data={}`
  → спецификация с нулями, оплата пуста, тихо (без исключения). Дропдаун был сломан
  **всегда**, просто не замечали. Фикс: при выборе в дропдауне дозагружать полный
  снапшот через `get_kp_by_number` (симметрично ручному пути), обёрнут в `st.cache_data`
  по `kp_number` (без лишних запросов к Supabase на каждый rerun). Два пути (дропдаун и
  ручной номер) теперь дают идентичный результат.

**Фича «открыть прошлый КП» — ЗАКРЫТА (07-06).** Менеджер находит прошлое КП по номеру
или контрагенту → конфигуратор восстанавливается в точности как перед генерацией →
правит → сохраняет как версию (43562 → 43562.1). Решает главную продакшен-боль: данные
КП больше не теряются между сессиями. Детали — в секции ниже.

**Парсер реквизитов (label-anchored) — закрыт пока (07-05).** Стал работать заметно лучше
(банк / директор / основание), остаточные проблемы есть — доработка отдельной сессией,
вынесена в техдолг. Не блокер.

**Аудиты (read-only):** архитектурный (`AUDIT_2026-07.md`) — **применён (07-07)**, P0/P1
закрыты, см. секцию ниже. UI/UX (Playwright-отчёт, затрагивает реквизиты) — ждёт применения
после доработки парсера.

**Админ-панель прайса — отдельный трек.** Фазы 1 + 1.5 закрыты и протеганы
(`v2.3-admin-phase1`). Фазы 2–5 — по карте `docs/ADMIN_PANEL_*.md`, не начаты.

Теги: `v2.2`, `v2.3-admin-phase1`, `v2.4`. Пост-v2.4 гигиена + фиксы сверки (07-07)
запушены в `main` без отдельного тега — при желании пометить `v2.4.1`.

---

## 🟡 Сверка сценариев генерации — ФРОНТ АКТИВЕН (07-07)

Ручная сверка генерации КП/спецификации/договора против эталонных `.md` в корне
проекта. Гейт качества перед деплоем. Проверяет корректность scope→clauses по всем
веткам. **Верификатор — Антон (доменное знание), Claude парсит docx и диффует с
эталоном; механику флагует, смысловое эскалирует.**

**Тест-сьют:** `SCENARIO_TEST_SUITE.md` (в outputs, перенести в docs/ и коммитнуть) —
8 сценариев, покрывают все оси: фундамент (нет/подрядчик/материал заказчика/заказчик
сам/зимний), монтаж (обычный/шеф), поверка, ОРИОН, форма заказчика (ООО/ИП/АО), тип
договора (общий/поставка). Реквизиты с валидными контрольными суммами.

**Проверено:** сценарий 1 (рама+пандусы+монтаж), сценарий 2 (фундамент подрядчик+
монтаж+поверка). **Осталось:** 3-8 (приоритет 4 шеф-монтаж и 7 ОРИОН+фундамент —
максимум уникальных клауз).

### Найденные баги генерации (копить до консолидированного фронта, НЕ чинить по одному)

- **БАГ-1 (P0, доменный)** — рама+пандусы (фундамент не строится) получает клаузы
  `customer_builds_foundation` (строительное задание + контрольный лист фото) вместо
  клаузы про твёрдую площадку 30×4м (эталон `Автовесы_рама_монтаж` п.6). Обратная ветка
  (подрядчик строит, сценарий 2) — корректна → проблема локальна к scope «рама/пандусы
  без фундамента». Фикс — **Opus** (scope→clauses, edge-кейсы, не сломать соседние ветки).
- **БАГ-2 (P1, системный, 2/2)** — кавычки Заказчика прямые `"..."` вместо ёлочек `«...»`
  в общем договоре+спецификации. Фикс `ea24b50` был на шаблонах договора поставки, до
  общего флоу не дошёл. Сценарий 8 (поставка) — обратная проверка.
- **БАГ-3 (P2, системный, 2/2)** — задвоенная ёлочка `ООО «ТПК «Тензосила»»` (`»»`) в
  связном тексте спецификации; в договоре одинарная. Непоследовательный рендер.
- **БАГ-4 (P1, системный, 2/2)** — дублирование в преамбуле договора: `...именуемые
  совместно «Стороны», именуемые совместно «Стороны», заключили...`. Только в теле
  договора, воспроизводится идентично. Баг склейки шаблона.
- **?-1 (на проверку)** — возможное дублирование фразы «Выезд ... бригад» в спецификации
  (сценарий 2). Проверить глазами: одна фраза или две. Проверить на сценарии 7.
- **?-2 (на проверку)** — оба приложения спецификации сценария 2 с заголовком
  «Строительное задание на фундамент Весов». Для «подрядчик строит» контрольный лист от
  заказчика логически не нужен. Проверить: задвоен заголовок или файл целиком.

**Подтверждено корректным (контроль регресса при фиксах):** клаузы «подрядчик строит
фундамент» (площадка под монолит) — верны; телефон (форматы без префикса+4знач, «8»+4знач)
распознаётся и попадает в реквизиты; ИНН/КПП/ОГРН не слипаются; суммы бьются КП↔спец↔договор;
P1-5 warning не ложнит.

**Рекомендация по фиксам:** БАГ-1 — отдельный Opus-фронт. БАГ-2/3/4 — вероятно один
sonnet-фронт (общий шаблон договора, НЕ supply_contract), но сначала найти код общего флоу.

### Авто-сверка (Code генерит + диффует с эталоном) — ОТЛОЖЕНА
Рассмотрена и осознанно отложена. Причина: программного входа в генерацию мимо Streamlit
нет (логика вшита в `2_Договор.py`) → петля требует сперва выпилить бэкенд-слой = кусок
миграции FastAPI. Плюс Code не может верифицировать смысл (домен — только Антон). Вернуться
**после миграции**, когда программный вход появится сам собой; тогда петля станет
регресс-тестом бэкенда. Пока — ручная сверка, Claude парсит docx.

---

## 🟢 Применение аудита AUDIT_2026-07 — сессия 07-07

Дисциплина: один фронт за раз, диагноз фактами до фикса, перед каждым фиксом — проверка,
что находка ещё жива на текущем main (аудит от 07-05, код с тех пор двигался).

**P0-3 — страховка тихо-неполного документа ✅ (`ac62e43`).** `compose.py` и
`spec_v2_filler.py` теперь проверяют bool-возврат `_replace_marker_with_paragraphs` /
`_remove_marker`; supply-флоу в `2_Договор.py` после `compose_supply` вызывает
`get_unfilled_placeholders()` и показывает `st.warning`. Договор больше не может тихо
уйти с literal `{{PAYMENT_SECTION}}`.

**Кластер P1 (тихие сбои) ✅ — 3 атомарных коммита:**
- P1-1 (`4250e08`): `_load_model_and_deps` логирует исключение + `st.warning` вместо
  тихого `None` (раньше ТТХ/комплект молча не заполнялись).
- P1-2 (`0546b41`): `postgrest_client_timeout=10` через `ClientOptions`. Закрывает
  «tests/storage зависает». `lru_cache` клиента уже был.
- P1-3 (`b78faa5`): `_clear_prices_cache` глотает только `ImportError`/`AttributeError`
  (ожидаемый no-op вне Streamlit), остальное логирует — админ записал прайс, КП больше
  не считает по старому кэшу молча.

**P1-5 — предохранитель суммы поставки ✅ (`73f3ce2`).** `_SUSPECT_WORK_RE`
(`монтаж|поверк|пусконаладк|фундамент`, **откалиброван на 17 реальных КП / 78 позиций**)
+ `_suspect_names()`: если имя позиции похоже на монтаж/фундамент, но `payment_group` не
`foundation`/`installation_and_verification` — `st.warning` менеджеру. Ничего не
классифицирует и не пересчитывает. Калибровка убила ложные срабатывания до прода
(`бетон` ловил только «желе**зобетон**ный» в корректных пресетах — выпилен). Реальный
кейс из данных: КП 245620.1 «ПАК ОРИОН Стандарт (оборудование + шеф-монтаж)» уезжал в
сумму поставки — теперь ловится.

**Фикс 4 красных тестов фундамента ✅ (`51f50d2`).** `test_pdf_to_fundament_docx.py`
искал файл `…3 скц.docx` (с пробелом), а данные и прод-код (`fundament_lookup.py:129-135`,
`f"{n}скц.docx"`) — БЕЗ пробела. Прод был корректен, баг только в тесте-фикстуре.
Полный прогон впервые зелёный.

**id-leak (frame_18 / ramp_set_f_s / fence_norma_20) — разобран, чинить нечего ✅.**
Диагностика показала: клиентский рендер имён уже исправлен на main (`3a15785`), все 4 id
резолвятся в человекочитаемые labels. «Утечка в спецификацию» была лог-предупреждениями
скриптов (stderr), а не отрендеренным документом. Добавлены регресс-тесты (`523bbb5`)
на защиту от raw-id. Остаётся отложенным: mismatch `frame_`/`ramp_set_` vs `rama`/`pandus`
в `_option_key_to_spec_id` — влияет на scope-классификацию, НЕ на имена клиенту; по
последствиям безобиден (рама/пандус не несут обязательств сторон, в отличие от монтажа).

**P0-1 (regex телефона) ✅ (`980621f`).** `_PHONE_RE` расширен: код города 3-5 цифр,
вариант без префикса при якоре «тел»/«факс» + лог нераспознанного телефона. Подтверждён
живьём на сверке сценариев (форматы без префикса и с «8»).

---

## 🟢 Договор поставки — две волны фиксов

### Волна 1 — функциональные баги P1–P3 (06-29) ЗАКРЫТЫ

Прогон: КП «Завод деталей» ВЕСТА-С-60-20 → договор поставки. Код + два разовых
патч-скрипта (`scripts/patch_supply_appendix1_spec.py`, `scripts/patch_supply_headers.py`).

**P1 (блокеры)**
- [x] **Пустой блок оплаты (п.4.2).** Guard в `compose.py`: пустой `_payment_lines`
  → `_remove_marker`, иначе `_replace_marker_with_paragraphs`.
- [x] **Сумма поставки включала монтаж.** `supply_filler._supply_spec_rows`: фильтр по
  `payment_group` — исключены `installation_and_verification` и `foundation`, остаются
  `scales` + `delivery`. Доставка — отдельной строкой спецификации.

**P2 (важные)**
- [x] **Колонтитулы с чужими данными.** `patch_supply_headers.py`: header трёх шаблонов
  → `{{ДОГОВОР_НОМЕР}}` / `{{ДОГОВОР_ДАТА}}`.
- [x] **Несимметричные наименования сторон.** `_buyer_context`:
  `ПОКУПАТЕЛЬ_НАИМЕНОВАНИЕ` = `ЗАКАЗЧИК_КРАТКОЕ` (fallback к полному).
- [x] **Несимметричные подписанты.** `_buyer_context`: `ПОКУПАТЕЛЬ_ДИРЕКТОР_ФИО` =
  `ЗАКАЗЧИК_ДИРЕКТОР_ИНИЦИАЛЫ` / `director_initials(...)`.
- [x] **СРОК_ДЕЙСТВИЯ_ДО не из поля.** ✅ **Подтверждено перепрогоном (договор №21, 07-01):**
  ввод `29.12.2026` → п.9.1 показал `29.12.2026`. Литерала `30.12.2026` в шаблоне нет,
  проводка UI→manual→контекст корректна. Cleanup виджета (убран конфликтный `value=`
  у `date_input`) сделан.
- [x] **Спецификация одной строкой.** `patch_supply_appendix1_spec.py`: spec-таблица
  appendix_1 → row-цикл `{%tr for row in spec_rows %}` (как в appendix_2). Контекст
  `spec_rows`: весы + доставка.

**P3 (косметика)**
- [x] Двойная точка в адресе — `АДРЕС_ПОСТАВКИ` через `.rstrip(" .")`.
- [x] Пачка пробелов перед «п. 4.2» в п.3.2 — схлопнуты.

### Волна 2 — полировка форматирования и вёрстки (07-01) ЗАКОММИЧЕНО, ждёт ре-теста

5 коммитов поверх волны 1. **Все запушены**, ждут финального перепрогона.

- [x] **Нормализация форматирования** (`ea24b50`). Кавычки покупателя → ёлочки;
  инициалы подписантов → единый формат (`О.А. Фамилия`, без пробела между инициалами);
  сумма прописью в п.4.2.x → с заглавной (как в п.4.1).
  ⚠️ **Сверка 07-07 выявила:** фикс кавычек покрыл только supply-шаблоны, общий флоу
  договора всё ещё даёт прямые кавычки Заказчика (БАГ-2).

- [x] **Преамбула покупателя + 6 ключей** (`9d7657e`). Восстановлена клауза по образцу
  поставщика: «…в лице {должность РП} {ФИО РП}, действующего/ей на основании
  {основание}, именуемое/ый…». Путь A (авто-склонение): `petrovich` (уже в deps) для
  ФИО, словарь для должности, пол по отчеству. Парсер НЕ трогали — `ЗАКАЗЧИК_ОСНОВАНИЕ`
  и `ЗАКАЗЧИК_ТЕЛЕФОН` уже извлекались. 6 ключей в `_buyer_context` (всегда в контексте,
  пустые при отсутствии): `ПОКУПАТЕЛЬ_ДИРЕКТОР_ФИО_РП`, `_ДОЛЖНОСТЬ_РП`,
  `ПОКУПАТЕЛЬ_ДЕЙСТВУЕТ`, `ПОКУПАТЕЛЬ_ОСНОВАНИЕ`, `ПОКУПАТЕЛЬ_ИМЕНУЕМ`,
  `ПОКУПАТЕЛЬ_ТЕЛЕФОН`. Клауза под `{% if %}` (нет директора → скрыта), телефон под
  `{%p if %}` (нет → строка исчезает целиком).

- [x] **Платёжная вставка: шрифт** (`8c52ede`). п.4.2.x генерился Times New Roman 12
  вместо Arial 11. Корень: `_make_clause_para` создавал раны без копирования rPr
  маркера → падало в `docDefaults` шаблона (TNR12). Spec-флоу маскировал баг (его
  `docDefaults` = Arial11). Фикс в общей `_replace_marker_with_paragraphs`: копировать
  rPr рана маркера. Закрывает оба маркера (`PAYMENT_SECTION` + `TERMS_SECTION`) и оба
  флоу.

- [x] **Платёжная вставка: интервал** (`4341a91`). Тот же корень — не копировался pPr
  параграфа маркера → одинарный интервал вместо 1.5 и выравнивание. Фикс: копировать
  pPr маркера (`spacing line=360`, `jc=both`) на создаваемые параграфы.

- [x] **Вёрстка трёх шаблонов** (`974ce59`). 3 docx-файла, правка напрямую (python-docx),
  плейсхолдеры/`{%tr%}`-теги идентичны оригиналу, валидация passed:
  - `supply_contract.docx`: межсекционные отступы пустыми абзацами → реальные интервалы
    + `keepNext` (заголовки не виснут); **пустой лист перед приложением убран**
    (разрыв страницы в хвосте вместо 15 пустых абзацев); симметрия преамбулы (клауза
    «в лице» под условием) и реквизитов (метки «Юридический адрес»/«Банк:», порядок
    Банк→Р/с→К/с→БИК, строка «Тел./факс» под `{%p if %}`, email покупателя не жирный).
  - `supply_appendix_1.docx`: **`trHeight` спецификации** (2289 твипов → авто-высота,
    строки больше не раздуты); хвостовые пустые → разрыв страницы; разделитель между
    таблицей спецификации и таблицей подписей **сохранён**.
  - `supply_appendix_2.docx`: хвостовые пустые подчищены; разделители между таблицами
    (ТТХ / комплект / подписи) сохранены; `kit_rows`-цикл и ТТХ-таблица не трогали
    (были в норме).

> ⚠️ **Урок (зафиксировать):** между двумя таблицами в docx всегда должен оставаться
> ≥1 абзац — иначе Word сливает их в одну (поймано: грубая зачистка пустых абзацев
> приклеила подписи к таблице спецификации/комплекта; исправлено сохранением
> разделителей).

---

## ✅ Финальный перепрогон (07-01) — ПРОЙДЕН

Сгенерирован договор поставки на «Заводе деталей», проверен глазами:

- [x] Преамбула покупателя: «…в лице {Должность_РП} {ФИО_РП}, действующего/ей на
  основании Устава, именуемое/ый…» — род. падеж, пол, основание без двойного предлога.
- [x] Кейс без директора → клауза «в лице» скрыта; без телефона → строка скрыта.
- [x] Реквизиты: симметрия колонок, email не жирный, строка «Тел./факс» появляется
  (телефон покупателя починен позже, 07-05).
- [x] п.4.2.x: Arial 11 + интервал 1.5 + по ширине.
- [x] Пустых листов нет (ни перед спецификацией, ни перед ТТХ).
- [x] Спецификация: компактные строки, подписи отдельной таблицей (не слиты).
- [x] Приложение №2: ТТХ + комплект + подписи раздельно.

---

## 🟢 P0-01: дамп-скрипт DOCX — ЗАКРЫТ (07-06)

Первая задача фазы 0 миграции (docs/MIGRATION_PLAN.md §4). Новый пакет
`tests/golden/`: `dump_docx.py` (оркестратор + CLI) + `dump_props.py`
(whitelist-сводки) + `dump_walker.py` (рекурсивный lxml-обход, склейка ранов)
+ `dump_styles.py` (шапка [DEFAULTS]/[STYLE]/[NUM]/[MEDIA]) + 28 тестов.

- Канонический plain-text дамп: явные rPr/pPr (ловит регресс наследования —
  сигнатура багов 8c52ede/4341a91), vMerge/gridSpan/trHeight, textbox
  (mc:Choice, не Fallback), колонтитулы по секциям, порядок склейки
  docxcompose (pgbrk/PAGEBREAK), поля (PAGE), sha1 медиа. Игнорирует rsid,
  docProps, rId; стили по именам, numId по алиасам. Спецификация формата
  v1 — в докстринге `dump_docx.py`.
- Приёмка: двойной прогон на 3 реальных DOCX (КП, спецификация v2, договор
  поставки) — sha1 дампов попарно идентичны; мутация bold одного рана в
  копии → дифф ровно 1 содержательная строка (`R{}` → `R{b}`).
- `pytest tests/ --ignore=tests/contracts/synthetic`: 966 passed, 1 skipped.
- **Ревью-фиксы (0388316):** codex недоступен (лимит до 27.07) → мульти-агентное
  ревью (8 углов). Закрыто: потеря instr незакрытого поля (TOC), утечка tab/br
  из кэша поля, тихая потеря текста в w:sdt/w:ins/w:smartTag и noBreakHyphen/sym,
  KeyError на висячем headerReference (→ BROKEN-REF), basedOn-родители в [STYLE],
  1 ZipFile вместо 3 чтений, файлы приведены к ≤200 (RunWalker → dump_runs.py,
  тесты → 4 файла). Отклонено осознанно: doc.sections API (чтение линкованного
  колонтитула МУТИРУЕТ документ), удаление fldSimple/w:cr (текстонесущие).
- Следующие задачи фазы 0: P0-02 (фикстуры G-01…G-09; в т.ч. кейс с vMerge —
  в текущих смоук-КП слияния сроков нет), P0-03 (заморозка эталонов, freezegun).

## 🟢 Фича «открыть прошлый КП» — ЗАКРЫТА (07-06)

Менеджер переоткрывает ранее сгенерированный КП, правит, сохраняет как версию.

- **Поиск:** по номеру (`get_kp_by_number`) и по контрагенту (`search_kps_by_contractor`,
  ilike) + список последних (`list_recent_kps`). UI-блок `src/ui/load_kp_section.py`.
- **Восстановление:** новый модуль `src/storage/kp_restore.py` — `reconstruct_kp_state()`
  (чистая функция снапшот→state) + `apply_kp_snapshot_to_state()` (применение в
  session_state). Читает снапшот ПОЛНОСТЬЮ, включая суффиксные widget-ключи
  `opt_*__{model_id}` — иначе цена/qty/сторона опций терялись бы на первом рендере
  (виджет перезатирал canonical дефолтом). Договорный `from_kp._reconstruct_state` НЕ трогали.
- **Дополнен снапшот:** `total_term_days` + `_user_set` (ручная правка срока раньше
  терялась). `model_price` не меняли — в живом потоке уже хранится фактическим числом
  (заморозка цены, нет дрейфа при изменении прайса).
- **Legacy graceful:** старые КП без новых полей грузятся с дефолтами (пересчёт срока,
  розница из прайса), не падают. Миграцию не делали — нечего мигрировать.
- **Версии вручную:** номер `.1` вводит менеджер (43562 → 43562.1). Предупреждение о
  перезаписи, пока номер не сменён (`save_kp` = upsert по номеру, иначе оригинал затрётся).
- **Проверено:** round-trip, legacy, freeze цены, ручной smoke на реальном КП. Запушено.

Файлы: `kp_restore.py` (нов.), `load_kp_section.py` (нов.), `snapshot_builder.py` (+2 поля),
`1_Коммерческое_предложение.py` (вызов), тесты.

---

## ✅ Корневой баг маппинга позиций (custom_N) — ЗАКРЫТ ДИАГНОСТИКОЙ (07-07)

Диагностирован при разборе автовыбора. **Резолюция: механизм подтверждён на уровне кода,
но НЕ воспроизводится на реальных данных — понижен до P2.**

**Что проверено (2 read-only прогона, src/ не тронут):**
- Прогон реального КП 242424: монтаж/фундамент пришли **штатными ключами**
  (`install_default`, `foundation_s_f_20`) → канонические id → scope проставился →
  8 клауз обязательств сработали. custom-позиций с потерянным scope — 0.
- Скан всего корпуса (17 КП / 78 позиций): **0 позиций монтаж/фундамент/поверка
  улетело в `custom_`.** Механизм бага в БД не воспроизводится ни разу.

**Гипотеза аудита «~90% договоров теряют обязательства» — ОПРОВЕРГНУТА.** Это была
подмена частоты: «монтаж есть в 90% договоров» ≠ «монтаж приходит как custom». Штатный
путь (чекбокс монтажа → `install_default`) даёт корректный scope. custom-позиция монтажа
возникает только при ручном добавлении мимо чекбокса — на текущем корпусе таких нет.

**Механизм (для истории, если всплывёт):** `from_kp.py:484-489,538-554` — неизвестный
ключ / custom_item → `id="custom_N"`, scope НЕ пишется; `clauses_context.py:54-98` —
строгий id-lookup, custom не видит. Если бы монтаж пришёл как custom — обязательства
молча пропали бы.

**Предохранитель добавлен (P1-5, `73f3ce2`):** если позиция монтаж/фундамент осталась
вне `foundation`/`installation_and_verification` — менеджер видит `st.warning`.

**Остаток (отложен):** `_option_key_to_spec_id` не знает префиксы `frame_`/`ramp_set_`
(проверяет `rama`/`pandus`) → рама/пандус уходят в `custom_` без scope. Влияет на
scope-классификацию, НЕ на имена клиенту; безобиден по последствиям (рама/пандус не
несут обязательств сторон). Не трогать без боли.

**Автовыбор типа договора — ОТЛОЖЕН.** Менеджер ставит тип вручную.

---

## 🟢 Парсер реквизитов (label-anchored) — ЗАКРЫТ ПОКА (07-05)

> Остаточные проблемы есть — доработка отдельной сессией (в техдолге). Не блокер.

**Развилка решена:** формат вставки — полуструктурированный текст с русскими метками
(«ИНН: X КПП: Y» в одну строку). Это идеальный кейс для label-anchored парсинга, DaData
не нужен (избыточен, гонял бы реквизиты клиентов наружу). Подход — двухслойный:
(1) резка по меткам-якорям (значение = от метки до следующей метки),
(2) валидация по форме (ИНН \d{10}|\d{12}, БИК \d{9}, счета \d{20}) как страховка.

**История заходов:**
- Заход 1 (`c519aad`) — **откачен**. Sonnet-исполнение по Opus-плану налепило дыр:
  банк рвался на кавычках, основание жрало телефон, «Директор» попадал в ФИО.
  Урок: резка сегментов — задача «думать по ходу», не «выполнить по плану» → Opus, не opus-plan.
- Заход 2 — **закрыт (07-05)** (xhigh, plan). Тест-корпус: 4 реальные карточки
  (Ромашка/ИП Сидорова/Вектор/Гранит) с явными ожиданиями по каждому полю.
  Три корня: (1) банк — метка «Банк» только как начало поля, не граница; сегмент до
  следующей реальной метки, кавычки не участвуют; (2) слитный директор — срезать
  должность-приставку до ФИО; (3) основание — до метки, не до точки.

**ИНН — НЕ баг парсера.** Гейт по контрольной сумме (`_valid_inn`) верный и сохраняется:
битый ИНН в юр-документ пускать нельзя. Мои тестовые ИНН были невалидны по чек-сумме —
в тестах заменены на валидные, поведение не трогаем.

**Побочно закрыто:** залипание полей реквизитов — не очищались между «Распознать»,
показывали данные прошлой карточки. Фикс через `set_requisites` (replace + сброс
виджетов `w_*` + пересчёт пола). Проверено: 4 карточки подряд без перезагрузки — чисто.

⚠️ **После закрытия парсера:** проверить/применить UI/UX аудит (Playwright-отчёт) — он
затрагивает в т.ч. страницу реквизитов.

---

## Активная задача

**Сверка сценариев генерации** (см. секцию выше) — идёт. Осталось сценарии 3-8.

**Очередь (по порядку):**
1. **Завершить сверку сценариев 3-8** → консолидированный фронт фиксов багов генерации
   (БАГ-1 Opus, БАГ-2/3/4 sonnet). Гейт качества перед раздачей людям.
2. **Количество автовесов > 1** — последняя механическая доработка КП. Разведка сделана
   (~7 точек: `state`, виджет, снять хардкод `1` ×3, домножить комплект Прил.№2, убрать
   «на единицу продукции»). Паттерн `price × qty` уже обкатан на опциях. Обходится вручную
   на странице Договора — НЕ блокер пилота.
3. **Деплой (Этап 2) → Streamlit Cloud** (решено 07-07). Настоящий гейт запуска. Отдельная
   сессия: секреты/env, домен, лимиты Cloud. Делать после сверки.
4. **UI/UX аудит** (Playwright-отчёт) — применить.

**Крупные фронты потом:**
- Доработка парсера реквизитов (остаточные проблемы, см. техдолг).
- Админ-панель Фаза 2. Tender-модуль.
- Миграция FastAPI + React (фаза 0 начата — golden dump готов). После миграции —
  авто-сверка сценариев как регресс-тест бэкенда.

---

## Админ-панель прайса (PDF-first) — отдельный трек

### Фаза 1 ЗАКРЫТА ✅ + Фаза 1.5 (чистка) ЗАКРЫТА ✅

Тег `v2.3-admin-phase1`. 92 admin-теста зелёные, все codex review чистые, запушено.

- **Фаза 1 (PDF-first):** цикл «загрузил 2 PDF → merge+дедуп → таблица (правка) →
  валидация (блокеры/предупреждения) → diff в бизнес-языке → backup+atomic write →
  откат (1 уровень)». Backend — чистые функции (`src/admin/price_pdf_dealer.py`,
  `price_pdf_retail.py`, `price_pdf_merge.py`, `price_business_summary.py`,
  `price_write_service.py`, `price_validation_split.py`), UI тонкий.
  Ключевые решения: дилерская цена из PDF напрямую (не формулой ×0.92); опоры 3х/4х —
  только 4х (3х выведен с 2026); ОРИОН — одной суммой с `individual_calc`; ключи моделей
  неизменяемы; three-way merge сохраняет JSON-only позиции без PDF-эквивалента.
- **Фаза 1.5 (чистка):** удалены 44 файла `tasks/AP-000…043`, 3 старых `admin_panel_*.md`,
  4 мёртвые ветки.

⚠️ **Нумерация:** «Фаза 1» (PDF-first) ≠ старая «Фаза 1/2» из enterprise-карты
`AP-001…043` (заморожена). Актуальная карта работ — `docs/ADMIN_PANEL_ARCHITECTURE.md` /
`_PHASES_TASKS.md` / `_REVIEW_CHECKLIST.md`.

### Дорожная карта — Фазы 2–5 (НЕ начаты)

- **Фаза 2 — Ценовые правила.** Коэффициенты вилки из `config.py` →
  `data/pricing_rules.json`, правка в UI, КП их читает. ⚠️ Риск: КП завязан на числа
  коэффициентов — нужна регрессия расчёта.
- **Фаза 3 — Бизнес-диагностика и защита.** Подсветка аномалий (скачок цены >X%,
  осиротевшая опция, расхождение вне вилки), предупреждение о просроченном прайсе.
  ⚠️ Риск: ложные срабатывания.
- **Фаза 4 — UX-полировка.** Только то, что всплыло в эксплуатации.
- **Фаза 5 — Регрессия и безопасный выпуск.** e2e КП и договора на обновлённом прайсе, тег.

Отложено (enterprise, по конкретной боли, заранее НЕ проектируем): история версий,
управление `payment_terms.json`, роли, аудит-лог, согласования, БД-версионирование.

### Техдолг трека админ-панели

- [ ] **EOL `data/prices.json`:** `write_prices` пишет LF, репо в CRLF → после каждого
  применения прайса в UI файл фантомно-modified. Лечится `.gitattributes`
  (`data/prices.json text eol=lf`) или записью в нужном EOL.
- [ ] Синтетические `case_*` фикстуры (5 падающих e2e) — **те самые, что всплывают в
  `git status` как modified.** Маскируют реальные падения в полном прогоне. Откатывать
  `git restore`, не коммитить вслепую; разобрать в контрактном треке.
- [ ] `road_slabs`/`pag_slabs`: `price_class=B_retail_only`, но есть `price_dealer_ru`.
- [ ] `price_upload_*` — переоценить после PDF-флоу (НЕ удалять, завязан на `3_Админка.py`).

---

## Аудиты (read-only)

### Архитектурный аудит → `AUDIT_2026-07.md` — ПРИМЕНЁН (07-07)
Мета-вывод: **архитектура здоровая**, проблемы — локальные баги тихих сбоев, не
структурная гниль (прямой ответ на импульс «всё отрефакторить»). Статусы находок:
- P0-1 (regex телефона) — ✅ сделан (`980621f`).
- P0-2 (custom_N) — ✅ **закрыт диагностикой**: не воспроизводится на 17 КП, понижен до
  P2, предохранитель P1-5 добавлен.
- P0-3 (незаполненные плейсхолдеры — safety net) — ✅ сделан (`ac62e43`).
- Кластер P1 (тихие сбои) — ✅ сделан: P1-1 (`4250e08`), P1-2 (`0546b41`), P1-3 (`b78faa5`).
- P1-5 (предохранитель суммы поставки) — ✅ сделан (`73f3ce2`).
- P2 (data_loader dedup, разбить 1019-строчную страницу договора) — **отложено как
  overengineering**, не трогать без боли.

### UI/UX аудит → Playwright MCP (диагностический отчёт)
Прогон сценария выбор модели → генерация КП через Playwright MCP (read-only, код не
трогался). **Проверить/применить ПОСЛЕ парсера** — затрагивает страницу реквизитов.

---

## Технический долг

### Баги генерации договоров (из сверки сценариев 07-07 — консолидированный фронт)
- [ ] **БАГ-1 (P0):** рама+пандусы → чужая ветка клауз фундамента вместо площадки. Opus.
- [ ] **БАГ-2 (P1):** кавычки Заказчика прямые вместо ёлочек в общем флоу договора.
- [ ] **БАГ-3 (P2):** задвоенная ёлочка `«Тензосила»»` в связном тексте спецификации.
- [ ] **БАГ-4 (P1):** дублирование «именуемые совместно Стороны» в преамбуле договора.
- [ ] **?-1 / ?-2:** на проверку глазами (дублирование фразы выезда бригад; заголовки
  двух приложений). Подробности — секция «Сверка сценариев».

### Из codex review v2.2
- [ ] P2: `payment_lines_editor` группирует % по тексту «Объект» → стабильный
  bucket/source id.
- [ ] P3: `docs/supply_payment_preview.txt` → пересохранить UTF-8.

### Баги
- [ ] UI Договор: Ctrl+V в «Номер КП»; поиск по Заказчику.
- [x] ~~«Последние КП»: краш float('') на битых снапшотах~~ — ЗАКРЫТ (1aafd37).
- [x] ~~«Последние КП»: КП выбирается, но спецификация/оплата пусты~~ — ЗАКРЫТ: дропдаун
  не тянул `data`, дозагрузка `get_kp_by_number` + `st.cache_data`. Отдельный старый баг,
  не связан с 1aafd37.
- [x] ~~Шаг 8: имена файлов с пробелом («…3 скц.docx» vs «…3скц.docx»)~~ — ЗАКРЫТ
  (`51f50d2`): прод-код был корректен, баг был в тест-фикстуре.
- [ ] 4–5 падающих `test_e2e_synthetic` (fixtures устарели от генератора).
- [x] ~~`tests/storage` зависает при сетевых проблемах~~ — ЗАКРЫТ (P1-2 timeout, `0546b41`).

### Рендеринг / DOCX
- [ ] vMerge ячеек в таблице спецификации DOCX (отдельно от `trHeight`, который закрыт).
- [ ] P3: temp-файл не удаляется при падении `fill_template` в `compose.py`.
- [ ] P3: `merge_runs` guard защищает `w:drawing`, но не `w:pict`/`w:object`/field-runs.
- [ ] `ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА`: константа «не более 50 м».

### Косметика clauses
- [ ] `section_number` в `clauses.yaml` рендерером не используется.
- [ ] `winter_concrete` пересмотреть после scope-редизайна (связано с зимним сценарием 5).

### Классификация позиций
- [ ] `_option_key_to_spec_id`: mismatch `frame_`/`ramp_set_` vs `rama`/`pandus` →
  рама/пандус в `custom_` без scope. Безобиден (не несут обязательств), но при
  scope-редизайне свести с custom_N. S.

### Платежи / реквизиты
- [ ] **Парсер реквизитов (label-anchored) — остаточные проблемы после захода 2.**
  Стал заметно лучше, но не идеален — доработка отдельной сессией.
- [ ] Ручные суммы-копейки в редакторе оплаты — режим поверх B.
- [ ] Natasha для ФИО/адресов — опция, если regex слаб. Не сейчас.

### Крупные рефакторинги
- [ ] `data_loader`: дублирование `equipment_specs` ↔ `models.json`.
- [ ] AI-extractor → DeepSeek после стабилизации.
- [ ] Терминология `ЗАКАЗЧИК_*` → `КОНТРАГЕНТ_*` — большая задача, не сейчас.

### Фичи (бэклог)
- [ ] Ручная спецификация с загрузкой стройзадания (переиспользует
  `pdf_to_fundament_docx.py`).
- [ ] Реальное удаление позиций прайса через UI (сейчас no-op).
- [ ] Автовыбор типа договора — после решения по `custom_N`.
- [ ] Зимний режим фундамента (+обогрев бетона) и «материал заказчика» — проверить,
  покрыты ли (сценарии 5, 6). Если нет — вопрос охвата MVP, не баг.

### Инфраструктура
- [ ] `use_container_width` → `width=`. ⚠️ Дедлайн 2025-12-31 ПРОШЁЛ — проверить.
- [ ] `docs/cloud_claude_handoff_admin_price_ui.md` — untracked, отдельная тема
  (хэндофф админ-панели), закоммитить отдельным коммитом или удалить.

> Техдолг трека админ-панели (EOL prices.json, фикстуры case_*, road_slabs/pag_slabs,
> price_upload_*) — вынесен в секцию «Админ-панель прайса» выше.

### Решено
- [x] Сверка сценариев 1-2 + тест-сьют на 8 сценариев (`SCENARIO_TEST_SUITE.md`) — 07-07.
- [x] P0-3 safety net + кластер P1 (тихие сбои) + P1-5 предохранитель — сессия 07-07.
- [x] custom_N закрыт диагностикой (не воспроизводится на 17 КП, понижен до P2).
- [x] 4 красных теста фундамента (`…3скц` без пробела, тест-фикстура).
- [x] id-leak (frame_18/ramp_set/fence_norma) — рендер имён уже чинён на main (3a15785),
  добавлены регресс-тесты (523bbb5).
- [x] Фича «открыть прошлый КП» + версии `.1` (`kp_restore` + `load_kp_section` + снапшот).
- [x] `ПОКУПАТЕЛЬ_ТЕЛЕФОН` в реквизитах договора (regex парсера + маппинг + плейсхолдер).
- [x] «Последние КП» — ДВА бага: (1) краш `float('')` на битых снапшотах (`1aafd37`);
  (2) дропдаун не тянул `data` из БД → пустой договор — дозагрузка снапшота + `st.cache_data`.
- [x] Залипание полей реквизитов между «Распознать» (set_requisites + сброс виджетов).
- [x] CodeGraph MCP зависал при codex review — обход
  `codex review --commit <SHA> -c 'mcp_servers.codegraph.enabled=false'`. В CLAUDE.md.
- [x] Платёжная вставка TNR12/одинарный → Arial11/1.5 (наследование rPr+pPr маркера).
- [x] Раздутые строки спецификации (`trHeight` appendix_1).
- [x] Пустой лист перед Приложением №1 (разрыв в хвосте `supply_contract`).

---

## Известные ограничения

- `named_form` (преамбула «именуемое/ый») покрывает ИП vs ООО/АО/ПАО/ЗАО/НАО.
  Колхоз/Компания/Фирма и пр. → «именуемое» (не всегда грамматически точно). Принято
  как ограничение MVP.
- Между двумя таблицами docx нужен ≥1 абзац-разделитель, иначе Word их сливает
  (см. урок выше).
- ОРИОН-приложения (Шаг 10 договоров) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- `монолитная_плита`: без стройзадания нет унифицированных чертежей.
- Supply-флоу на втором движке (docxtpl) — при изменениях шаблонов отдельная проверка
  плейсхолдеров + колонтитулов.
- Удаление позиций прайса через UI — пока no-op.
- Автовыбор типа договора не работает корректно (`custom_N`) — тип ставится вручную.

---

## Деплой и горизонт

- **Деплой → Streamlit Cloud** (решено 07-07). После сверки сценариев. Отдельная сессия:
  секреты/env, домен, лимиты Cloud.
- FastAPI + React + Bitrix24 (REST) — после закрепления Streamlit.
- Tender-модуль (RAG по архиву ТЗ) — после договоров.

---

## Теги версий

- `v1.2` — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup Шаг 8.
- `v2.1` — оплата для всех пресетов + редактор B + docxcompose + читаемые имена + модуль
  реквизитов.
- `v2.2` — договор поставки + 3 сессии стабилизации + codex review fixes.
- `v2.3-admin-phase1` — админ-панель прайса PDF-first: парсеры + merge + запись + UI +
  откат.
- `v2.4` — договор поставки доведён (вёрстка шаблонов, преамбула РП + 6 ключей, симметрия
  реквизитов, телефон, шрифт/интервал платёжки) + регресс «Последние КП» + фича «открыть
  прошлый КП» с версиями `.1`.
- (пост-v2.4, 07-07, в `main` без тега) — применение аудита (P0-3 + кластер P1 + P1-5 +
  фикс 4 тестов фундамента + custom_N диагностикой) + сверка сценариев 1-2.

---

## Команды

```powershell
git status
python -m pytest tests/ -v
streamlit run src/app.py
```