# STATUS — Tenzosila KP & Dogovor

Последнее обновление: 2026-06-27

---

## Где мы сейчас

**Ручная проверка Части 17 идёт (Антон).** Прогон договора поставки на реальном
КП вскрыл пакет багов шаблона поставки + диагностирован корневой баг маппинга
позиций (custom_N). Автовыбор типа договора — ОТЛОЖЕН (см. ниже), менеджер
ставит тип вручную. Активный фронт на паузе до решения по шаблону поставки.

Теги: `v2.2` (договор поставки + стабилизация), `v2.3-admin-phase1` (админ-панель).

---

## 🔴 Договор поставки — баги шаблона (найдены на ручном прогоне 2026-06-27)

Прогон: КП «Завод деталей» ВЕСТА-С-60-20 → договор поставки. Источник правды по
багам — этот прогон + сверка с КП.

### P1 (блокеры)
- [ ] **Пустой блок оплаты (п.4.2).** Платёжные строки не отрендерились —
  после «Покупатель осуществляет оплату в следующем порядке:» пусто.
  Договор без условий оплаты нерабочий. Причина не подтверждена (payment_lines
  не доходят до шаблона / цикл не сработал) — нужна диагностика.
- [ ] **Сумма поставки включает монтаж.** п.4.1 = 2 460 000 (весы 2 200 000 +
  монтаж 190 000 + доставка 70 000). РЕШЕНО продуктово: сумма поставки =
  весы + доставка, работы (монтаж/фундамент/поверка/курирование) исключаются.
  Доставка — отдельной строкой в спецификации.

### P2 (важные)
- [ ] **Колонтитулы с чужими данными.** В footer «№ 86/2026 от 26.04.2026»
  (литералы из образца), в теле «№ 26/2026 от 27.06.2026». docxtpl по
  умолчанию не рендерит header/footer — нужен отдельный проход по колонтитулам.
- [ ] **Несимметричные наименования сторон.** Поставщик — краткое
  «ООО «ТПК «Тензосила»», Покупатель — полное «Общество с ограниченной…».
  В преамбуле, реквизитах, подписях. Привести покупателя к КРАТКОМУ
  наименованию (ЗАКАЗЧИК_КРАТКОЕ).
- [ ] **Несимметричные подписанты.** Поставщик «О.А. Сенаторов» (инициалы),
  Покупатель «Соколов Андрей Викторович» (полностью). Привести к одному
  формату (инициалы + фамилия).
- [ ] **СРОК_ДЕЙСТВИЯ_ДО не из поля.** В договоре 30.12.2026, хотя поле
  заполнялось. valid_until не прокидывается из UI в шаблон — проверить связку.
- [ ] **Спецификация одной строкой.** Товар + сумма одной строкой, без разбивки
  весы / доставка отдельными позициями. Сумма опять с монтажом (см. P1).

### P3 (косметика)
- [ ] Двойная точка в адресе («…Текстильщиков 24..»).
- [ ] Пачка пробелов перед «п. 4.2» в п.3.2 (мусор из образца).

---

## 🟡 Корневой баг маппинга позиций (custom_N) — ОТЛОЖЕН, но важен

Диагностирован при разборе автовыбора. Шире, чем автовыбор.

- [ ] **Монтаж из конфигуратора КП сохраняется как custom-item (id="custom_N"),
  без metadata["scope"].** build_clauses_context ищет строго
  items_by_id["installation"] → не находит → installation_scope="none".
  Аналогично foundation (id="foundation").
  Цепочка: from_kp.py:538-554 (build_specification_items, ветка custom) →
  clauses_context.py:54-65.
- **Последствия:**
  1. Автовыбор типа предлагает «Поставку» при наличии монтажа (монтаж невидим
     для scope). → из-за этого автовыбор и отложен.
  2. **ПОД ВОПРОСОМ (проверить!):** scope-обязательства в spec-договоре для
     монтажа/фундамента, когда они идут как custom. Монтаж в ~90% договоров.
     Если обязательства по монтажу НЕ появляются на реальных spec-договорах —
     баг крупный (затрагивает основной флоу). Если появляются — значит путь
     id="installation" где-то отрабатывает, и custom_N только про автовыбор.
  3. Монтаж попадает в сумму поставки (есть как позиция, scope не считается).
- **Решение НЕ принято.** Кандидат: определять scope по payment_group
  (installation_and_verification) вместо строгого id. Нюанс: payment_group
  объединяет монтаж И поверку — нужен под-признак чтобы не путать
  (диагностика по поверке не завершена).
- **Автовыбор типа договора — ОТЛОЖЕН** до решения по этому корню. Менеджер
  ставит тип вручную (он и так знает, нужен ли договор поставки).

**Следующий шаг по spec-договору (когда вернёшься к проверке):** проверить на
реальном КП с монтажом — появляются ли в договоре-спецификации обязательства
по монтажу. Это разводит «custom_N только автовыбор» vs «custom_N ломает
основной флоу».

---

## ⚠️ Висящие ручные проверки (Часть 17)

- [x] Договор поставки — прогнан, баги найдены (см. раздел выше).
- [ ] Старый spec-флоу — не сломан ли договор-спецификация (+ проверка
  обязательств по монтажу, см. custom_N выше).
- [ ] Реквизиты — частично: на прогоне ИНН и Банк не распознались при чистой
  вставке (см. техдолг «парсер реквизитов»).
- [ ] Платежи (0% по весам, десятичные %, ±1 руб).
- [ ] Binary/layout review DOCX и PDF-фикстур.

---

## Парсер реквизитов — поднять качество (стратегическое, отдельный трек)

На прогоне: при вставке чистых реквизитов ИНН и Банк остались пустыми (данные
в тексте были). Много ручной доработки. Развилка (НЕ решена):
- Допилить regex на большем корпусе реальных карточек (дёшево, детерминированно).
- DaData API: по ИНН тянуть всё из ЕГРЮЛ (снимает ~90% ручной работы, но
  сетевая зависимость — ранее отклонялась).
- Гибрид: локальный парсер ловит ИНН → DaData дозаполняет → менеджер проверяет.
Обсудить отдельно, не смешивать с фиксами шаблона.

---

## Активная задача

**Пауза по решению Антона.** Работу с автовыбором/custom_N отложили (риск
разрастания). Приоритет когда вернёмся — **переделка шаблона договора поставки**
(пакет P1-P2 выше), это изолированно и не трогает корневой маппинг.

Порядок при возврате:
1. Диагностика P1 (почему пустая оплата, почему колонтитулы не рендерятся).
2. Opus-сессия: фикс шаблона поставки пакетом (оплата, колонтитулы, симметрия
   наименований/подписантов, срок, спецификация весы+доставка без монтажа).
3. Перепрогон на чистом поставочном КП.
4. Позже отдельно: парсер реквизитов (стратегия), custom_N (корень).

---

## Админ-панель прайса (PDF-first) — Фаза 1 ЗАКРЫТА ✅

Тег `v2.3-admin-phase1`. 92 admin-теста зелёные, все codex review чистые.
[раздел без изменений — детали в предыдущей версии STATUS и git]

---

## Технический долг

### Договор поставки (новое, 2026-06-27)
- [ ] Пакет багов шаблона поставки — см. раздел 🔴 выше (P1-P3).
- [ ] custom_N маппинг позиций — см. раздел 🟡 выше (отложен, проверить влияние
  на spec-флоу).
- [ ] Парсер реквизитов: ИНН/Банк не распознались при чистой вставке — см.
  раздел выше (стратегический трек).

### Из codex review v2.2
- [ ] P2: payment_lines_editor группирует % по тексту «Объект» → стабильный bucket/source id.
- [ ] P2: synthetic e2e baseline не подтверждён (5/5 failing).
- [ ] P3: docs/supply_payment_preview.txt → пересохранить UTF-8.

### Баги
- [ ] Шаг 8: имена файлов с пробелом («...3 скц.docx» vs «...3скц.docx»).
- [ ] UI Договор: Ctrl+V в «Номер КП»; поиск по Заказчику.
- [ ] «Последние КП» не грузит snapshot.
- [ ] 4–5 падающих test_e2e_synthetic (fixtures устарели от генератора).
- [ ] tests/storage зависает при сетевых проблемах.

### Рендеринг / DOCX
- [ ] vMerge ячеек в таблице спецификации DOCX.
- [ ] P3: temp-файл не удаляется при падении fill_template в compose.py.
- [ ] P3: merge_runs guard защищает w:drawing, но не w:pict/w:object/field-runs.
- [ ] ТТХ_РАССТОЯНИЕ_ДО_ТЕРМИНАЛА: константа «не более 50 м».

### Косметика clauses
- [ ] section_number в clauses.yaml рендерером не используется.
- [ ] winter_concrete пересмотреть после scope-редизайна.

### Платежи / реквизиты
- [ ] Ручные суммы-копейки в редакторе оплаты — режим поверх B.
- [ ] Natasha для ФИО/адресов — опция, если regex слаб. Не сейчас.

### Крупные рефакторинги
- [ ] data_loader: дублирование equipment_specs ↔ models.json.
- [ ] AI-extractor → DeepSeek после стабилизации.
- [ ] Терминология ЗАКАЗЧИК_* → КОНТРАГЕНТ_* — большая задача, не сейчас.

### Фичи (бэклог)
- [ ] Ручная спецификация с загрузкой стройзадания (переиспользует pdf_to_fundament_docx.py).
- [ ] Реальное удаление позиций прайса через UI (сейчас no-op).
- [ ] Автовыбор типа договора — после решения по custom_N.

### Инфраструктура
- [ ] use_container_width → width=. ⚠️ Дедлайн 2025-12-31 ПРОШЁЛ — проверить.

### Трек админ-панели — Фаза 1.5 (чистка)
- [ ] road_slabs/pag_slabs: price_class=B_retail_only, но есть price_dealer_ru.
- [ ] Чистка по описи R5.
- [ ] price_upload_* — переоценить после PDF-флоу.

### Решено
- [x] CodeGraph MCP зависал при codex review — обход
      `codex review --commit <SHA> -c 'mcp_servers.codegraph.enabled=false'`. В CLAUDE.md.

---

## Известные ограничения

- ОРИОН-приложения (Шаг 10 договоров) — не реализованы как пакет документов.
- Контрольный лист: только пандусный и приямок.
- монолитная_плита: без стройзадания нет унифицированных чертежей.
- Supply-флоу на втором движке (docxtpl) — при изменениях шаблонов отдельная
  проверка плейсхолдеров + колонтитулов.
- Удаление позиций прайса через UI — пока no-op.
- Автовыбор типа договора не работает корректно (custom_N) — тип ставится вручную.

---

## Деплой и горизонт

- Деплой Streamlit Cloud / VPS — после стабилизации.
- FastAPI + React + Bitrix24 (REST) — после закрепления Streamlit.
- Tender-модуль (RAG по архиву ТЗ) — после договоров.

---

## Теги версий

- v1.2 — библиотека фундаментов 23 DOCX + конвертер PDF→DOCX + lookup Шаг 8.
- v2.1 — оплата для всех пресетов + редактор B + docxcompose + читаемые имена + модуль реквизитов.
- v2.2 — договор поставки + 3 сессии стабилизации + codex review fixes.
- v2.3-admin-phase1 — админ-панель прайса PDF-first: парсеры + merge + запись + UI + откат.

---

## Команды

\```powershell
git status
rtk python -m pytest tests/ -v
streamlit run src/app.py
\```