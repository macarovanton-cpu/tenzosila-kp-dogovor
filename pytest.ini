[pytest]
testpaths = tests
markers =
    e2e: end-to-end through Playwright (requires Streamlit subprocess + browser)
    visual: visual regression on rendered DOCX (requires LibreOffice + poppler)
addopts = -m "not e2e" --ignore=tests/e2e
