# NEXT_TASK → HUMAN_REVIEW_PHASE_2_BEFORE_PHASE_3

Фаза 2 админ-панели прайсов выполнена кодом:

- AP-008 — shell страницы админки;
- AP-015 — read-only панель текущего прайса и правил;
- AP-010 — безопасная проверка загруженного JSON-прайса: validate + diff +
  download без записи в `data/` и БД.

## Перед следующей задачей

Нужен human review всей Фазы 2, потому что задачи Streamlit-UI имеют уровень
безопасности `requires_human_review_after`. Фазу 3 не начинать до явной команды
человека.

Проверить вручную:

- `streamlit run src/app.py`;
- открыть страницу «Админка»;
- убедиться, что видны counts, metadata, errors, warnings и правила классов;
- сверить НДС 22%, `MAX_COEFF`, `MIN_COEFF_B`,
  `SYNTHETIC_DEALER_FACTOR` с `src/config.py`;
- загрузить копию `data/prices.json` в раздел проверки прайса;
- убедиться, что diff пустой и доступна кнопка скачивания проверенного JSON;
- загрузить заведомо битый JSON и убедиться, что видны errors, а скачивание
  заблокировано;
- убедиться, что страница read-only и не предлагает активацию прайса, запись в
  `data/` или сохранение в БД;
- проверить, что `data/prices.json` на диске не изменился;
- убедиться, что «Коммерческое предложение» и «Договор» открываются как раньше.

## Следующий этап после review

После ручной проверки человек решает, нужен ли вход в Фазу 3. Без отдельной
команды не начинать БД, Supabase, миграции, active-loader или изменение pricing
logic.
